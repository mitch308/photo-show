---
phase: 01-项目基础架构
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/package.json
  - backend/tsconfig.json
  - backend/vite.config.ts
  - backend/src/index.ts
  - backend/src/app.ts
  - backend/src/config/database.ts
  - backend/src/config/redis.ts
  - backend/src/config/index.ts
  - backend/src/models/Admin.ts
  - backend/src/types/response.ts
  - backend/.env.example
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]
user_setup:
  - service: MySQL
    why: "Primary database for admin data"
    env_vars:
      - name: DB_HOST
        source: "MySQL server host"
      - name: DB_PORT
        source: "MySQL server port (default 3306)"
      - name: DB_USER
        source: "MySQL username"
      - name: DB_PASSWORD
        source: "MySQL password"
      - name: DB_NAME
        source: "Database name to create/use"
  - service: Redis
    why: "JWT blacklist and session storage"
    env_vars:
      - name: REDIS_HOST
        source: "Redis server host"
      - name: REDIS_PORT
        source: "Redis server port (default 6379)"
must_haves:
  truths:
    - "Backend server can start without errors"
    - "Database connection succeeds with valid credentials"
    - "Redis connection succeeds"
    - "Admin table exists in database"
  artifacts:
    - path: backend/src/index.ts
      provides: "Server entry point"
      exports: ["app", "server"]
    - path: backend/src/config/database.ts
      provides: "TypeORM MySQL connection"
      contains: "createConnection"
    - path: backend/src/config/redis.ts
      provides: "Redis client"
      contains: "createClient"
    - path: backend/src/models/Admin.ts
      provides: "Admin entity model"
      contains: "@Entity"
  key_links:
    - from: backend/src/index.ts
      to: database.ts
      via: "import and initialize"
      pattern: "import.*database"
    - from: backend/src/index.ts
      to: redis.ts
      via: "import and initialize"
      pattern: "import.*redis"
---

<objective>
Initialize backend project with Express, TypeScript, Vite, TypeORM, and Redis. Create the foundation for authentication system.

**Purpose:** Establish backend infrastructure that supports admin authentication with persistent storage and session management.

**Output:** Runnable backend server with database and Redis connections, Admin entity model, and environment configuration.
</objective>

<execution_context>
@C:/workspace/photo-show/.opencode/get-shit-done/workflows/execute-plan.md
@C:/workspace/photo-show/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/01-项目基础架构/01-CONTEXT.md

## Key Decisions to Implement

- **D-01:** Monorepo structure with backend/ directory
- **D-05:** Unified API response format: `{ code: number, data: any, message: string }`
- **D-07:** Use .env files for configuration
- **D-08:** Provide .env.example

## Pitfall Prevention (from PITFALLS.md)

- **Database connection pool:** Configure TypeORM with proper pool settings to prevent connection leaks
- **Redis retry strategy:** Implement reconnection logic for Redis
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Backend Project</name>
  <files>backend/package.json, backend/tsconfig.json, backend/vite.config.ts, backend/src/index.ts, backend/src/app.ts, backend/src/types/response.ts</files>
  <action>
    Initialize a Node.js backend project with TypeScript and Vite build tool.

    **Steps:**
    1. Create `backend/` directory
    2. Create `package.json` with dependencies:
       - express, cors, dotenv
       - typeorm, mysql2, redis (ioredis)
       - bcrypt, jsonwebtoken
       - Dev: typescript, @types/*, ts-node, nodemon, vite
    3. Create `tsconfig.json` with strict mode, ES2022 target, Node module
    4. Create `vite.config.ts` for backend build (library mode not needed, use for dev server)
    5. Create `src/types/response.ts` with unified response format per D-05:
       ```typescript
       export interface ApiResponse<T = any> {
         code: number;    // 0=success, non-zero=error code
         data: T;
         message: string;
       }
       ```
    6. Create `src/app.ts` with Express app setup (cors, json parser, error handler)
    7. Create `src/index.ts` as entry point that loads dotenv and starts server

    **Avoid:**
    - Don't use CommonJS imports - use ES modules with TypeScript
    - Don't enable synchronize: true in production config
  </action>
  <verify>
    <automated>cd backend && npm install && npm run build 2>&1 | grep -q "error" && echo "FAIL" || echo "PASS"</automated>
  </verify>
  <done>Backend project compiles successfully with `npm run build`</done>
</task>

<task type="auto">
  <name>Task 2: Configure Database Connection</name>
  <files>backend/src/config/database.ts, backend/src/config/index.ts, backend/.env.example</files>
  <action>
    Set up TypeORM connection to MySQL with proper connection pooling.

    **Per D-07 and D-08:**
    1. Create `.env.example` with all required environment variables:
       ```
       # Server
       PORT=3000
       NODE_ENV=development

       # Database
       DB_HOST=localhost
       DB_PORT=3306
       DB_USER=root
       DB_PASSWORD=your_password
       DB_NAME=photo_show

       # Redis
       REDIS_HOST=localhost
       REDIS_PORT=6379

       # JWT
       JWT_SECRET=your-secret-key-change-in-production
       JWT_EXPIRES_IN=7d
       JWT_REFRESH_EXPIRES_IN=30d
       ```
    2. Create `src/config/index.ts` to load and export env vars with validation
    3. Create `src/config/database.ts` with TypeORM DataSource:
       - Use environment variables for connection config
       - Configure connection pool: min 2, max 10 connections
       - Set acquire timeout and idle timeout
       - Enable logging in development mode

    **Pitfall prevention:** Configure pool settings to avoid "Too many connections" error.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit src/config/database.ts 2>&1 | grep -q "error" && echo "FAIL" || echo "PASS"</automated>
  </verify>
  <done>TypeScript compiles database config without errors; .env.example contains all required variables</done>
</task>

<task type="auto">
  <name>Task 3: Configure Redis Connection</name>
  <files>backend/src/config/redis.ts</files>
  <action>
    Set up Redis client with proper retry strategy.

    1. Create `src/config/redis.ts` using ioredis
    2. Implement retry strategy with max retries and backoff
    3. Add connection event handlers (connect, error, close)
    4. Export the client for use in auth middleware (JWT blacklist)

    **Per PITFALLS.md:** Configure retryStrategy and handle connection errors gracefully.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit src/config/redis.ts 2>&1 | grep -q "error" && echo "FAIL" || echo "PASS"</automated>
  </verify>
  <done>Redis client module compiles and exports connection with retry logic</done>
</task>

<task type="auto">
  <name>Task 4: Create Admin Entity Model</name>
  <files>backend/src/models/Admin.ts</files>
  <action>
    Create TypeORM entity for Admin user.

    1. Create `src/models/Admin.ts` with:
       ```typescript
       @Entity('admins')
       export class Admin {
         @PrimaryGeneratedColumn('uuid')
         id: string;

         @Column({ unique: true })
         username: string;

         @Column()
         password: string;  // bcrypt hashed

         @Column({ nullable: true })
         email?: string;

         @CreateDateColumn()
         createdAt: Date;

         @UpdateDateColumn()
         updatedAt: Date;
       }
       ```
    2. Ensure model is registered in DataSource entities

    **Per D-04:** This entity will be seeded with default admin on initialization.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit src/models/Admin.ts 2>&1 | grep -q "error" && echo "FAIL" || echo "PASS"</automated>
  </verify>
  <done>Admin entity compiles; table structure will be created on first run</done>
</task>

</tasks>

<verification>
1. Run `npm install` in backend/ - should complete without errors
2. Run `npm run build` - should compile successfully
3. Copy .env.example to .env with real credentials
4. Run `npm run dev` - server should start and connect to DB/Redis
</verification>

<success_criteria>
- [ ] Backend project compiles with TypeScript
- [ ] .env.example exists with all required variables
- [ ] TypeORM DataSource configured with connection pool
- [ ] Redis client configured with retry strategy
- [ ] Admin entity model defined
</success_criteria>

<output>
After completion, create `.planning/phases/01-项目基础架构/01-01-SUMMARY.md`
</output>
---
phase: 15-后台筛选
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/routes/admin/share.ts
  - backend/src/services/shareService.ts
  - frontend/src/api/share.ts
  - frontend/src/views/admin/Shares.vue
autonomous: true
requirements:
  - AUX-04
user_setup: []
must_haves:
  truths:
    - "管理员可以在分享管理页面看到客户筛选下拉框"
    - "管理员可以在分享管理页面看到类型筛选下拉框（作品分享/相册分享）"
    - "筛选后结果正确过滤"
    - "筛选时显示加载状态"
  artifacts:
    - path: "frontend/src/views/admin/Shares.vue"
      provides: "分享管理页面筛选 UI"
      contains: "clientFilter, typeFilter"
    - path: "backend/src/routes/admin/share.ts"
      provides: "分享 API 筛选参数支持"
      exports: ["clientId", "type"]
    - path: "backend/src/services/shareService.ts"
      provides: "分享服务筛选逻辑"
      contains: "listAllShares.*filter"
  key_links:
    - from: "frontend/src/views/admin/Shares.vue"
      to: "/api/admin/share"
      via: "shareApi.getShares with filters"
      pattern: "loadShares.*clientId.*type"
    - from: "backend/src/routes/admin/share.ts"
      to: "shareService.listAllShares"
      via: "query parameters"
      pattern: "clientId.*type"
---

<objective>
实现分享管理页面的筛选功能，支持按客户和分享类型筛选。

Purpose: 让管理员快速定位特定分享链接。
Output: Shares.vue 页面包含客户筛选和类型筛选下拉框，后端 API 支持筛选参数。
</objective>

<execution_context>
@.opencode/get-shit-done/workflows/execute-plan.md
@.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-后台筛选/15-CONTEXT.md

## 现有代码参考

### 当前 Shares API (backend/src/routes/admin/share.ts)
```typescript
router.get('/', async (req: Request, res: Response) => {
  const shares = await shareService.listAllShares();
  // 需要添加: clientId, type 筛选参数
});
```

### ShareTokenData 结构 (backend/src/services/shareService.ts)
```typescript
export interface ShareTokenData {
  workIds?: string[];   // 作品分享
  albumId?: string;     // 相册分享（与 workIds 互斥）
  clientId?: string;    // 客户 ID
  // ...
}
```

### 当前 shareApi.getShares (frontend/src/api/share.ts)
```typescript
async getShares(): Promise<ShareInfo[]>
// 需要添加: clientId, type 可选参数
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展后端分享 API 支持筛选</name>
  <files>
    backend/src/routes/admin/share.ts,
    backend/src/services/shareService.ts
  </files>
  <read_first>
    backend/src/routes/admin/share.ts,
    backend/src/services/shareService.ts
  </read_first>
  <action>
    扩展分享 API 以支持客户和类型筛选：

    1. **修改 backend/src/routes/admin/share.ts**：
       - 在 GET /api/admin/share 路由中，从 req.query 提取 clientId 和 type 参数
       - 将筛选参数传递给 shareService.listAllShares：
         ```typescript
         router.get('/', async (req: Request, res: Response) => {
           try {
             const { clientId, type } = req.query;
             const shares = await shareService.listAllShares({
               clientId: clientId as string | undefined,
               type: type as 'work' | 'album' | undefined,
             });
             res.json(successResponse(shares));
           } catch (error: any) {
             console.error('Error in GET /api/admin/share:', error);
             res.status(500).json(errorResponse(ErrorCodes.UNKNOWN, error.message));
           }
         });
         ```

    2. **修改 backend/src/services/shareService.ts**：
       - 修改 listAllShares 方法签名，添加可选筛选参数：
         ```typescript
         async listAllShares(filters?: {
           clientId?: string;
           type?: 'work' | 'album';
         }): Promise<ShareInfo[]>
         ```
       - 在返回结果前应用筛选：
         ```typescript
         // 在 shares.sort 之前添加筛选逻辑
         if (filters?.clientId) {
           shares = shares.filter(share => share.clientId === filters.clientId);
         }
         if (filters?.type === 'work') {
           shares = shares.filter(share => !share.albumId);
         }
         if (filters?.type === 'album') {
           shares = shares.filter(share => !!share.albumId);
         }
         ```

    参考 D-11: GET /api/admin/share?clientId=xxx&type=work/album
  </action>
  <verify>
    <automated>cd backend && npm run build 2>&1 | grep -E "error|Error" || echo "Build successful"</automated>
  </verify>
  <done>
    - backend/src/routes/admin/share.ts 包含 clientId 和 type 查询参数提取
    - backend/src/services/shareService.ts 包含筛选逻辑
    - TypeScript 编译通过
  </done>
</task>

<task type="auto">
  <name>Task 2: 实现分享管理页面筛选 UI</name>
  <files>
    frontend/src/api/share.ts,
    frontend/src/views/admin/Shares.vue
  </files>
  <read_first>
    frontend/src/api/share.ts,
    frontend/src/views/admin/Shares.vue,
    frontend/src/stores/clients.ts
  </read_first>
  <action>
    在分享管理页面添加筛选功能：

    1. **修改 frontend/src/api/share.ts**：
       - 修改 getShares 方法签名，添加可选筛选参数：
         ```typescript
         async getShares(filters?: {
           clientId?: string;
           type?: 'work' | 'album';
         }): Promise<ShareInfo[]> {
           const params = new URLSearchParams();
           if (filters?.clientId) params.append('clientId', filters.clientId);
           if (filters?.type) params.append('type', filters.type);
           const url = params.toString() ? `/admin/share?${params.toString()}` : '/admin/share';
           const response = await api.get<ApiResponse<ShareInfo[]>>(url);
           return response.data.data;
         }
         ```

    2. **修改 frontend/src/views/admin/Shares.vue**：
       - 在 page-header 的 button 之前添加筛选控件：
         ```vue
         <div class="filters">
           <select v-model="clientFilter" class="filter-select">
             <option value="">全部客户</option>
             <option v-for="client in clients" :key="client.id" :value="client.id">
               {{ client.name }}
             </option>
           </select>
           <select v-model="typeFilter" class="filter-select">
             <option value="">全部类型</option>
             <option value="work">作品分享</option>
             <option value="album">相册分享</option>
           </select>
         </div>
         ```
       - 添加响应式变量：
         ```typescript
         const clientFilter = ref('');
         const typeFilter = ref('');
         ```
       - 添加筛选样式：
         ```css
         .filters {
           display: flex;
           gap: 12px;
         }
         
         .filter-select {
           padding: 8px 12px;
           border: 1px solid var(--border-color);
           border-radius: 4px;
           background: var(--bg-primary);
           min-width: 120px;
         }
         ```
       - 添加 watch 监听筛选变化（立即搜索，无防抖）：
         ```typescript
         watch([clientFilter, typeFilter], () => {
           loadShares();
         });
         ```
       - 修改 loadShares 函数以传递筛选参数：
         ```typescript
         const loadShares = async () => {
           loading.value = true;
           try {
             shares.value = await shareApi.getShares({
               clientId: clientFilter.value || undefined,
               type: typeFilter.value as 'work' | 'album' | undefined,
             });
           } finally {
             loading.value = false;
           }
         };
         ```

    参考 D-04: 客户筛选 + 类型筛选
    参考 D-07: 显示加载状态
    注意：下拉框筛选不需要防抖，改变即筛选
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | grep -E "error|Error" || echo "Build successful"</automated>
  </verify>
  <done>
    - Shares.vue 包含客户筛选和类型筛选下拉框
    - 筛选结果正确过滤
    - loading 状态正确反映
    - TypeScript 和 Vue 编译通过
  </done>
</task>

</tasks>

<verification>
1. 启动后端服务：`cd backend && npm run dev`
2. 启动前端服务：`cd frontend && npm run dev`
3. 登录管理后台
4. 进入分享管理页面
5. 选择特定客户，验证只显示该客户的分享
6. 选择"作品分享"，验证只显示作品分享
7. 选择"相册分享"，验证只显示相册分享
8. 清除筛选条件，验证恢复显示所有分享
</verification>

<success_criteria>
- 管理员可以在分享管理页面看到客户筛选下拉框
- 管理员可以看到类型筛选下拉框（作品分享/相册分享）
- 选择客户后，只显示该客户的分享
- 选择类型后，只显示对应类型的分享
- 筛选时表格显示加载状态
</success_criteria>

<output>
After completion, create `.planning/phases/15-后台筛选/15-04-SUMMARY.md`
</output>
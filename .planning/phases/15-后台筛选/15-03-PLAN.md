---
phase: 15-后台筛选
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/api/tags.ts
  - frontend/src/views/admin/Tags.vue
autonomous: true
requirements:
  - AUX-03
user_setup: []
must_haves:
  truths:
    - "管理员可以在标签管理页面看到名称搜索框"
    - "输入名称后 300ms 自动搜索，结果正确过滤"
    - "搜索时显示加载状态"
  artifacts:
    - path: "frontend/src/views/admin/Tags.vue"
      provides: "标签管理页面搜索 UI"
      contains: "searchQuery, watch"
    - path: "frontend/src/api/tags.ts"
      provides: "标签 API 搜索参数传递"
      contains: "getTags.*q"
  key_links:
    - from: "frontend/src/views/admin/Tags.vue"
      to: "/api/tags"
      via: "tagsApi.getTags with q"
      pattern: "loadTags.*q"
    - from: "frontend/src/api/tags.ts"
      to: "backend/src/routes/tags.ts"
      via: "q query parameter"
      pattern: "q.*LIKE"
---

<objective>
实现标签管理页面的搜索功能，支持按名称模糊搜索。

Purpose: 让管理员快速定位特定标签。
Output: Tags.vue 页面包含名称搜索框，前端 API 调用已有的后端搜索接口。

Note: 后端 tags API 已支持 `q` 参数搜索，只需添加前端 UI。
</objective>

<execution_context>
@.opencode/get-shit-done/workflows/execute-plan.md
@.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-后台筛选/15-CONTEXT.md

## 现有代码参考

### 后端 tags API 已支持搜索 (backend/src/routes/tags.ts)
```typescript
router.get('/', async (req: Request, res: Response) => {
  const { q } = req.query;
  const tags = q 
    ? await tagService.searchTags(q as string)
    : await tagService.getTags();
  res.json(successResponse(tags));
});
```

### tagService.searchTags 已实现 (backend/src/services/tagService.ts)
```typescript
async searchTags(query: string): Promise<Tag[]> {
  return this.tagRepo.find({
    where: { name: Like(`%${query}%`) },
    order: { name: 'ASC' },
  });
}
```

### 当前 frontend/src/api/tags.ts
```typescript
async getTags(): Promise<Tag[]>
// 需要添加: q 可选参数
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展前端标签 API 支持搜索参数</name>
  <files>
    frontend/src/api/tags.ts
  </files>
  <read_first>
    frontend/src/api/tags.ts,
    backend/src/routes/tags.ts
  </read_first>
  <action>
    扩展前端标签 API 以支持搜索参数：

    修改 frontend/src/api/tags.ts：
    - 修改 getTags 方法签名，添加可选参数 q?: string
    - 将 q 参数添加到 URL 查询字符串：
      ```typescript
      async getTags(q?: string): Promise<Tag[]> {
        const params = new URLSearchParams();
        if (q) params.append('q', q);
        const url = params.toString() ? `/tags?${params.toString()}` : '/tags';
        const response = await api.get<ApiResponse<Tag[]>>(url);
        return response.data.data;
      }
      ```

    参考 D-10: 标签 API 已支持搜索，前端只需添加搜索框
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | grep -E "error|Error" || echo "Build successful"</automated>
  </verify>
  <done>
    - frontend/src/api/tags.ts 的 getTags 方法支持 q 参数
    - TypeScript 编译通过
  </done>
</task>

<task type="auto">
  <name>Task 2: 实现标签管理页面搜索 UI</name>
  <files>
    frontend/src/views/admin/Tags.vue
  </files>
  <read_first>
    frontend/src/views/admin/Tags.vue,
    frontend/src/views/admin/Clients.vue
  </read_first>
  <action>
    在标签管理页面添加搜索功能：

    修改 frontend/src/views/admin/Tags.vue：
    
    1. 在 page-header 的 el-button 之前添加搜索框：
       ```vue
       <el-input
         v-model="searchQuery"
         placeholder="搜索标签名称..."
         clearable
         style="width: 200px; margin-right: 12px"
       />
       ```
    
    2. 添加响应式变量：
       ```typescript
       const searchQuery = ref('');
       ```
    
    3. 实现 300ms 防抖搜索：
       ```typescript
       let searchTimeout: ReturnType<typeof setTimeout>;
       
       watch(searchQuery, () => {
         clearTimeout(searchTimeout);
         searchTimeout = setTimeout(() => {
           loadTags(searchQuery.value || undefined);
         }, 300);
       });
       ```
    
    4. 修改 loadTags 函数以接受 q 参数：
       ```typescript
       async function loadTags(q?: string) {
         loading.value = true;
         try {
           tags.value = await tagsApi.getTags(q);
         } finally {
           loading.value = false;
         }
       }
       ```

    参考 D-03: 单一名称搜索
    参考 D-05: 使用 300ms 防抖
    参考 D-06: 不持久化到 URL
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | grep -E "error|Error" || echo "Build successful"</automated>
  </verify>
  <done>
    - Tags.vue 包含搜索框
    - 搜索有 300ms 防抖
    - loading 状态正确反映
    - TypeScript 和 Vue 编译通过
  </done>
</task>

</tasks>

<verification>
1. 启动后端服务：`cd backend && npm run dev`
2. 启动前端服务：`cd frontend && npm run dev`
3. 登录管理后台
4. 进入标签管理页面
5. 在搜索框输入标签名称关键词，验证搜索结果正确
6. 清除搜索条件，验证恢复显示所有标签
</verification>

<success_criteria>
- 管理员可以在标签管理页面看到名称搜索框
- 输入名称关键词后 300ms 自动搜索，结果正确
- 搜索时表格显示加载状态
</success_criteria>

<output>
After completion, create `.planning/phases/15-后台筛选/15-03-SUMMARY.md`
</output>
---
phase: 15-后台筛选
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/routes/albums.ts
  - backend/src/services/albumService.ts
  - frontend/src/api/albums.ts
  - frontend/src/views/admin/Albums.vue
autonomous: true
requirements:
  - AUX-02
user_setup: []
must_haves:
  truths:
    - "管理员可以在相册管理页面看到名称搜索框"
    - "输入名称后 300ms 自动搜索，结果正确过滤"
    - "搜索时显示加载状态"
  artifacts:
    - path: "frontend/src/views/admin/Albums.vue"
      provides: "相册管理页面搜索 UI"
      contains: "searchQuery, watch"
    - path: "backend/src/routes/albums.ts"
      provides: "相册 API 搜索参数支持"
      exports: ["name"]
    - path: "backend/src/services/albumService.ts"
      provides: "相册服务搜索逻辑"
      contains: "name LIKE"
  key_links:
    - from: "frontend/src/views/admin/Albums.vue"
      to: "/api/albums"
      via: "albumsApi.getAlbums with name"
      pattern: "loadAlbums.*name"
    - from: "backend/src/routes/albums.ts"
      to: "albumService.getAlbums"
      via: "name query parameter"
      pattern: "name.*LIKE"
---

<objective>
实现相册管理页面的搜索功能，支持按名称模糊搜索。

Purpose: 让管理员快速定位特定相册。
Output: Albums.vue 页面包含名称搜索框，后端 API 支持名称搜索参数。
</objective>

<execution_context>
@.opencode/get-shit-done/workflows/execute-plan.md
@.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-后台筛选/15-CONTEXT.md

## 现有代码参考

### 防抖搜索模式 (Clients.vue)
```typescript
let searchTimeout: ReturnType<typeof setTimeout>;
watch(searchQuery, () => {
  clearTimeout(searchTimeout);
  searchTimeout = setTimeout(() => {
    clientsStore.fetchClients({ search: searchQuery.value });
  }, 300);
});
```

### 当前 Albums API (backend/src/routes/albums.ts)
```typescript
router.get('/', async (req: Request, res: Response) => {
  const albums = await albumService.getAlbums();
  // 需要添加: name 参数
});
```

### 当前 albumService.getAlbums
```typescript
async getAlbums(): Promise<Album[]>
// 需要添加: name 可选参数
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展后端相册 API 支持名称搜索</name>
  <files>
    backend/src/routes/albums.ts,
    backend/src/services/albumService.ts
  </files>
  <read_first>
    backend/src/routes/albums.ts,
    backend/src/services/albumService.ts
  </read_first>
  <action>
    扩展相册 API 以支持名称搜索：

    1. **修改 backend/src/routes/albums.ts**：
       - 在 GET /api/albums 路由中，从 req.query 提取 name 参数
       - 将 name 参数传递给 albumService.getAlbums

    2. **修改 backend/src/services/albumService.ts**：
       - 修改 getAlbums 方法签名，添加可选参数 name?: string
       - 使用 queryBuilder 实现模糊搜索：
         ```typescript
         async getAlbums(name?: string): Promise<Album[]> {
           const query = this.albumRepo.createQueryBuilder('album')
             .leftJoinAndSelect('album.works', 'works');
           
           if (name) {
             query.andWhere('album.name LIKE :name', { name: `%${name}%` });
           }
           
           query.orderBy('album.position', 'ASC');
           return query.getMany();
         }
         ```

    参考 D-09: GET /api/albums?name=xxx
  </action>
  <verify>
    <automated>cd backend && npm run build 2>&1 | grep -E "error|Error" || echo "Build successful"</automated>
  </verify>
  <done>
    - backend/src/routes/albums.ts 包含 name 查询参数提取
    - backend/src/services/albumService.ts 包含 name LIKE 查询逻辑
    - TypeScript 编译通过
  </done>
</task>

<task type="auto">
  <name>Task 2: 实现相册管理页面搜索 UI</name>
  <files>
    frontend/src/api/albums.ts,
    frontend/src/views/admin/Albums.vue
  </files>
  <read_first>
    frontend/src/api/albums.ts,
    frontend/src/views/admin/Albums.vue
  </read_first>
  <action>
    在相册管理页面添加搜索功能：

    1. **修改 frontend/src/api/albums.ts**：
       - 修改 getAlbums 方法签名，添加可选参数 name?: string
       - 将 name 参数添加到 URL 查询字符串：
         ```typescript
         async getAlbums(name?: string): Promise<Album[]> {
           const params = new URLSearchParams();
           if (name) params.append('name', name);
           const url = params.toString() ? `/albums?${params.toString()}` : '/albums';
           const response = await api.get<ApiResponse<Album[]>>(url);
           return response.data.data;
         }
         ```

    2. **修改 frontend/src/views/admin/Albums.vue**：
       - 在 page-header 的 el-button 之前添加搜索框：
         ```vue
         <el-input
           v-model="searchQuery"
           placeholder="搜索相册名称..."
           clearable
           style="width: 200px; margin-right: 12px"
         />
         ```
       - 添加响应式变量：
         ```typescript
         const searchQuery = ref('');
         ```
       - 实现 300ms 防抖搜索：
         ```typescript
         let searchTimeout: ReturnType<typeof setTimeout>;
         
         watch(searchQuery, () => {
           clearTimeout(searchTimeout);
           searchTimeout = setTimeout(() => {
             loadAlbums(searchQuery.value || undefined);
           }, 300);
         });
         ```
       - 修改 loadAlbums 函数以接受 name 参数：
         ```typescript
         async function loadAlbums(name?: string) {
           loading.value = true;
           try {
             albums.value = await albumsApi.getAlbums(name);
           } finally {
             loading.value = false;
           }
         }
         ```

    参考 D-02: 单一名称搜索
    参考 D-05: 使用 300ms 防抖
    参考 D-06: 不持久化到 URL
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | grep -E "error|Error" || echo "Build successful"</automated>
  </verify>
  <done>
    - Albums.vue 包含搜索框
    - 搜索有 300ms 防抖
    - loading 状态正确反映
    - TypeScript 和 Vue 编译通过
  </done>
</task>

</tasks>

<verification>
1. 启动后端服务：`cd backend && npm run dev`
2. 启动前端服务：`cd frontend && npm run dev`
3. 登录管理后台
4. 进入相册管理页面
5. 在搜索框输入相册名称关键词，验证搜索结果正确
6. 清除搜索条件，验证恢复显示所有相册
</verification>

<success_criteria>
- 管理员可以在相册管理页面看到名称搜索框
- 输入名称关键词后 300ms 自动搜索，结果正确
- 搜索时表格显示加载状态
</success_criteria>

<output>
After completion, create `.planning/phases/15-后台筛选/15-02-SUMMARY.md`
</output>
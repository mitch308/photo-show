---
wave: 2
depends_on: [11-01]
files_modified:
  - frontend/src/views/admin/Settings.vue
requirements_addressed: [STUD-02]
autonomous: true
estimated_time: 20min
---

# Plan 11-02: 后台设置页面

**Requirement:** STUD-02 — 管理员可以使用富文本编辑器编辑工作室介绍内容

**Objective:** 在后台设置页面添加"工作室信息"卡片，包含基本信息表单、Logo 上传、富文本编辑器。

---

## Context

**Why:** 管理员需要一个界面来配置工作室信息，包括基本联系方式和富文本介绍。

**Key insight:** 扩展现有 Settings.vue 页面，新增卡片区域。使用 wangEditor 作为富文本编辑器，轻量且中文支持好。

---

## Tasks

### Task 1: 安装 wangEditor 依赖

<read_first>
- frontend/package.json — 当前依赖列表
</read_first>

<action>
在 `frontend` 目录下运行：

```bash
cd frontend && npm install @wangeditor/editor @wangeditor/editor-for-vue@next
```

- `@wangeditor/editor` — wangEditor 核心包
- `@wangeditor/editor-for-vue` — Vue 3 适配器
</action>

<acceptance_criteria>
- `frontend/package.json` 包含 `@wangeditor/editor` 依赖
- `frontend/package.json` 包含 `@wangeditor/editor-for-vue` 依赖
</acceptance_criteria>

---

### Task 2: 扩展 Settings.vue 数据模型

<read_first>
- frontend/src/views/admin/Settings.vue — 现有数据模型（约第 1-30 行）
- frontend/src/types/settings.ts — StudioInfo 类型
</read_first>

<action>
在 `frontend/src/views/admin/Settings.vue` 中：

1. 添加导入（约第 1-5 行）：
```typescript
import { ref, onMounted, shallowRef, onBeforeUnmount } from 'vue';
import { ElMessage } from 'element-plus';
import { settingsApi } from '@/api/settings';
import type { WatermarkConfig, StudioInfo } from '@/types/settings';
import { Editor, Toolbar } from '@wangeditor/editor-for-vue';
import type { IDomEditor, IEditorConfig, IToolbarConfig } from '@wangeditor/editor';
import '@wangeditor/editor/dist/css/style.css';
```

2. 添加工作室信息状态（在 watermarkConfig 相关变量后，约第 27 行后）：
```typescript
const studioForm = ref<StudioInfo>({
  name: '',
  logo: '',
  phone: '',
  email: '',
  address: '',
  description: ''
});
const studioSaving = ref(false);
const logoUploading = ref(false);

const editorRef = shallowRef<IDomEditor>();

const editorConfig: Partial<IEditorConfig> = {
  placeholder: '请输入工作室介绍...',
  MENU_CONF: {
    uploadImage: {
      async customUpload(file: File, insertFn: (url: string, alt: string, href: string) => void) {
        const formData = new FormData();
        formData.append('file', file);
        const result = await settingsApi.uploadStudioLogo(file);
        insertFn(result.logoPath, file.name, result.logoPath);
      }
    }
  }
};

const toolbarConfig: Partial<IToolbarConfig> = {
  excludeKeys: ['group-video']
};

function handleEditorCreated(editor: IDomEditor) {
  editorRef.value = editor;
}

onBeforeUnmount(() => {
  const editor = editorRef.value;
  if (editor) {
    editor.destroy();
  }
});
```

3. 在 onMounted 中加载工作室信息：
```typescript
onMounted(async () => {
  await loadConfig();
  await loadStudioInfo();
});

async function loadStudioInfo() {
  try {
    const info = await settingsApi.getStudioInfo();
    studioForm.value = info;
  } catch (error: any) {
    ElMessage.error(error.message || '加载工作室信息失败');
  }
}
```
</action>

<acceptance_criteria>
- `frontend/src/views/admin/Settings.vue` 包含 wangEditor 相关导入
- `frontend/src/views/admin/Settings.vue` 包含 `studioForm` ref
- `frontend/src/views/admin/Settings.vue` 包含 `editorRef` shallowRef
- `frontend/src/views/admin/Settings.vue` 包含 `handleEditorCreated` 函数
- `frontend/src/views/admin/Settings.vue` 包含 `loadStudioInfo` 函数
</acceptance_criteria>

---

### Task 3: 添加保存和上传方法

<read_first>
- frontend/src/views/admin/Settings.vue — 现有保存方法（约第 45-67 行）
</read_first>

<action>
在 `frontend/src/views/admin/Settings.vue` 中添加方法（在 handleImageUpload 后，约第 80 行后）：

```typescript
async function saveStudioInfo() {
  if (!studioForm.value.name.trim()) {
    ElMessage.warning('工作室名称不能为空');
    return;
  }

  studioSaving.value = true;
  try {
    await settingsApi.setStudioInfo(studioForm.value);
    ElMessage.success('工作室信息已保存');
  } catch (error: any) {
    ElMessage.error(error.message || '保存失败');
  } finally {
    studioSaving.value = false;
  }
}

async function handleLogoUpload(options: any) {
  logoUploading.value = true;
  try {
    const result = await settingsApi.uploadStudioLogo(options.file);
    studioForm.value.logo = result.logoPath;
    ElMessage.success('Logo上传成功');
  } catch (error: any) {
    ElMessage.error(error.message || '上传失败');
  } finally {
    logoUploading.value = false;
  }
}
```
</action>

<acceptance_criteria>
- `frontend/src/views/admin/Settings.vue` 包含 `saveStudioInfo` 方法
- `frontend/src/views/admin/Settings.vue` 包含 `handleLogoUpload` 方法
- `saveStudioInfo` 方法验证名称必填
</acceptance_criteria>

---

### Task 4: 添加工作室信息卡片模板

<read_first>
- frontend/src/views/admin/Settings.vue — 现有模板（约第 83-177 行）
</read_first>

<action>
在 `frontend/src/views/admin/Settings.vue` 的 `<template>` 部分，在水印设置卡片后（约第 176 行后）添加工作室信息卡片：

```vue
<el-card class="settings-card studio-card">
  <template #header>
    <div class="card-header">
      <span>工作室信息</span>
    </div>
  </template>

  <el-form :model="studioForm" label-width="100px">
    <el-form-item label="工作室名称" required>
      <el-input
        v-model="studioForm.name"
        placeholder="请输入工作室名称"
        maxlength="50"
        show-word-limit
      />
    </el-form-item>

    <el-form-item label="Logo">
      <div class="logo-upload-area">
        <el-upload
          :show-file-list="false"
          :http-request="handleLogoUpload"
          accept="image/*"
        >
          <el-button type="primary" :loading="logoUploading">
            {{ logoUploading ? '上传中...' : '上传Logo' }}
          </el-button>
        </el-upload>
        <div v-if="studioForm.logo" class="logo-preview">
          <el-image
            :src="studioForm.logo"
            fit="contain"
            style="max-width: 200px; max-height: 80px;"
          />
          <el-button
            type="danger"
            size="small"
            @click="studioForm.logo = ''"
            link
          >
            移除
          </el-button>
        </div>
      </div>
    </el-form-item>

    <el-form-item label="联系电话">
      <el-input
        v-model="studioForm.phone"
        placeholder="请输入联系电话"
        maxlength="20"
      />
    </el-form-item>

    <el-form-item label="邮箱">
      <el-input
        v-model="studioForm.email"
        placeholder="请输入邮箱地址"
        maxlength="50"
      />
    </el-form-item>

    <el-form-item label="地址">
      <el-input
        v-model="studioForm.address"
        placeholder="请输入工作室地址"
        maxlength="100"
      />
    </el-form-item>

    <el-form-item label="工作室介绍">
      <div style="border: 1px solid var(--el-border-color); border-radius: 4px; width: 100%;">
        <Toolbar
          :editor="editorRef"
          :defaultConfig="toolbarConfig"
          mode="simple"
          style="border-bottom: 1px solid var(--el-border-color);"
        />
        <Editor
          v-model="studioForm.description"
          :defaultConfig="editorConfig"
          mode="simple"
          style="height: 300px; overflow-y: hidden;"
          @onCreated="handleEditorCreated"
        />
      </div>
    </el-form-item>

    <el-form-item>
      <el-button type="primary" @click="saveStudioInfo" :loading="studioSaving">
        保存工作室信息
      </el-button>
    </el-form-item>
  </el-form>
</el-card>
```
</action>

<acceptance_criteria>
- `frontend/src/views/admin/Settings.vue` 包含工作室信息卡片
- 卡片包含名称输入框（必填）
- 卡片包含 Logo 上传区域
- 卡片包含联系方式表单项
- 卡片包含 wangEditor 富文本编辑器
- 卡片包含保存按钮
</acceptance_criteria>

---

### Task 5: 添加样式

<read_first>
- frontend/src/views/admin/Settings.vue — 现有样式（约第 180-241 行）
</read_first>

<action>
在 `frontend/src/views/admin/Settings.vue` 的 `<style scoped>` 部分添加：

```css
.studio-card {
  margin-top: 20px;
}

.logo-upload-area {
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.logo-preview {
  display: flex;
  align-items: center;
  gap: 10px;
}

.w-e-text-container {
  background: var(--el-bg-color);
}

.w-e-text-container [data-slate-editor] {
  min-height: 280px;
}
```
</action>

<acceptance_criteria>
- `frontend/src/views/admin/Settings.vue` 包含 `.studio-card` 样式
- `frontend/src/views/admin/Settings.vue` 包含 `.logo-upload-area` 样式
- `frontend/src/views/admin/Settings.vue` 包含 `.logo-preview` 样式
</acceptance_criteria>

---

## Verification

**Manual test:**
1. 启动前端 `cd frontend && npm run dev`
2. 登录后台，进入设置页面
3. 验证：
   - 页面显示"工作室信息"卡片
   - 名称输入框正常工作
   - Logo 上传正常工作
   - 联系方式表单正常工作
   - 富文本编辑器正常显示和输入
   - 保存后数据持久化（刷新页面后数据仍存在）

**Type check:**
```bash
cd frontend && npm run type-check
```

---

## Dependencies

- 11-01: 后端 API 和类型定义必须先完成

---

## Risks

| Risk | Mitigation |
|------|------------|
| wangEditor 样式与 Element Plus 冲突 | 使用 scoped 样式，检查 CSS 变量兼容性 |
| 富文本编辑器销毁内存泄漏 | 在 onBeforeUnmount 中正确销毁编辑器实例 |
| 图片上传失败 | 后端已配置 XSS 过滤，前端显示错误提示 |

---

*Plan created: 2026-03-26*
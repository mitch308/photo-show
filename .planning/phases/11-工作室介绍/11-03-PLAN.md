---
wave: 3
depends_on: [11-01, 11-02]
files_modified:
  - frontend/src/router/index.ts
  - frontend/src/views/About.vue
  - frontend/src/views/Home.vue
requirements_addressed: [STUD-03]
autonomous: true
estimated_time: 15min
---

# Plan 11-03: 前台介绍页面

**Requirement:** STUD-03 — 访客可以在前台查看工作室介绍页面

**Objective:** 创建独立 `/about` 路由页面展示工作室信息，并在首页 header 添加导航链接。

---

## Context

**Why:** 访客需要一个专门的页面了解工作室的基本信息、联系方式和介绍内容。

**Key insight:** 创建独立页面组件，复用 Home.vue 的 header 样式，使用 Element Plus 图标展示联系方式。

---

## Tasks

### Task 1: 创建 About.vue 页面组件

<read_first>
- frontend/src/views/Home.vue — 页面结构和 header 样式参考
- frontend/src/types/settings.ts — StudioInfo 类型
</read_first>

<action>
创建 `frontend/src/views/About.vue`：

```vue
<script setup lang="ts">
import { ref, onMounted } from 'vue';
import { Phone, Message, Location } from '@element-plus/icons-vue';
import { settingsApi } from '@/api/settings';
import type { StudioInfo } from '@/types/settings';

const studioInfo = ref<StudioInfo>({
  name: '',
  phone: '',
  email: '',
  address: '',
  description: ''
});
const loading = ref(true);

onMounted(async () => {
  try {
    const info = await settingsApi.getStudioInfo();
    studioInfo.value = info;
  } catch (error) {
    console.error('Failed to load studio info:', error);
  } finally {
    loading.value = false;
  }
});
</script>

<template>
  <div class="about-page" v-loading="loading">
    <header class="header">
      <h1>{{ studioInfo.name || '摄影工作室' }}</h1>
      <nav>
        <router-link to="/">首页</router-link>
        <router-link to="/login">管理登录</router-link>
      </nav>
    </header>
    
    <main class="main" v-if="!loading">
      <div class="studio-header">
        <img 
          v-if="studioInfo.logo" 
          :src="studioInfo.logo" 
          :alt="studioInfo.name"
          class="logo"
        />
        <h1 class="title">{{ studioInfo.name }}</h1>
      </div>
      
      <div class="contact-info" v-if="studioInfo.phone || studioInfo.email || studioInfo.address">
        <div v-if="studioInfo.phone" class="contact-item">
          <el-icon :size="20"><Phone /></el-icon>
          <span>{{ studioInfo.phone }}</span>
        </div>
        <div v-if="studioInfo.email" class="contact-item">
          <el-icon :size="20"><Message /></el-icon>
          <a :href="`mailto:${studioInfo.email}`">{{ studioInfo.email }}</a>
        </div>
        <div v-if="studioInfo.address" class="contact-item">
          <el-icon :size="20"><Location /></el-icon>
          <span>{{ studioInfo.address }}</span>
        </div>
      </div>
      
      <div 
        v-if="studioInfo.description" 
        class="description"
        v-html="studioInfo.description"
      ></div>
      
      <div v-if="!studioInfo.name && !studioInfo.description" class="empty-state">
        <p>暂无工作室介绍信息</p>
      </div>
    </main>
  </div>
</template>

<style scoped>
.about-page {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  background: var(--bg-primary, #f5f5f5);
}

.header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 20px 40px;
  background: var(--bg-card, #ffffff);
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
}

.header h1 {
  font-size: 24px;
  font-weight: 600;
  color: var(--text-primary, #333);
  margin: 0;
}

.header nav {
  display: flex;
  gap: 16px;
}

.header nav a {
  padding: 8px 16px;
  background: var(--color-primary, #409eff);
  color: white;
  border-radius: 4px;
  text-decoration: none;
  transition: opacity 0.2s;
}

.header nav a:hover {
  opacity: 0.8;
}

.main {
  flex: 1;
  padding: 40px;
  max-width: 800px;
  margin: 0 auto;
  width: 100%;
}

.studio-header {
  display: flex;
  flex-direction: column;
  align-items: center;
  margin-bottom: 32px;
}

.logo {
  max-width: 200px;
  max-height: 100px;
  object-fit: contain;
  margin-bottom: 16px;
}

.title {
  font-size: 32px;
  font-weight: 600;
  color: var(--text-primary, #333);
  margin: 0;
}

.contact-info {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 24px;
  margin-bottom: 40px;
  padding: 20px;
  background: var(--bg-card, #ffffff);
  border-radius: 8px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
}

.contact-item {
  display: flex;
  align-items: center;
  gap: 8px;
  color: var(--text-secondary, #666);
}

.contact-item a {
  color: var(--color-primary, #409eff);
  text-decoration: none;
}

.contact-item a:hover {
  text-decoration: underline;
}

.description {
  padding: 24px;
  background: var(--bg-card, #ffffff);
  border-radius: 8px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
  line-height: 1.8;
  color: var(--text-primary, #333);
}

.description :deep(h1) {
  font-size: 24px;
  margin: 0 0 16px 0;
}

.description :deep(h2) {
  font-size: 20px;
  margin: 16px 0 12px 0;
}

.description :deep(h3) {
  font-size: 16px;
  margin: 12px 0 8px 0;
}

.description :deep(p) {
  margin: 0 0 12px 0;
}

.description :deep(ul), .description :deep(ol) {
  padding-left: 24px;
  margin: 0 0 12px 0;
}

.description :deep(img) {
  max-width: 100%;
  height: auto;
  border-radius: 4px;
  margin: 12px 0;
}

.empty-state {
  padding: 60px 20px;
  text-align: center;
  color: var(--text-secondary, #999);
}

@media (max-width: 768px) {
  .header {
    padding: 16px 20px;
    flex-direction: column;
    gap: 12px;
  }
  
  .header h1 {
    font-size: 20px;
  }
  
  .main {
    padding: 20px 16px;
  }
  
  .title {
    font-size: 24px;
  }
  
  .contact-info {
    flex-direction: column;
    align-items: flex-start;
    padding: 16px;
  }
}
</style>
```
</action>

<acceptance_criteria>
- `frontend/src/views/About.vue` 文件存在
- 包含 `studioInfo` ref 和 `loading` ref
- 包含 `getStudioInfo` API 调用
- 包含 header 导航（首页、管理登录）
- 包含联系方式展示（图标 + 文字）
- 包含富文本介绍展示（v-html）
- 包含空状态处理
- 包含响应式样式
</acceptance_criteria>

---

### Task 2: 添加路由配置

<read_first>
- frontend/src/router/index.ts — 现有路由配置
</read_first>

<action>
在 `frontend/src/router/index.ts` 中：

1. 在 Home 路由后（约第 10 行后）添加 About 路由：
```typescript
{
  path: '/about',
  name: 'About',
  component: () => import('@/views/About.vue'),
  meta: { title: '关于我们' },
},
```
</action>

<acceptance_criteria>
- `frontend/src/router/index.ts` 包含 `/about` 路由
- 路由 `name` 为 `About`
- 路由 `meta.title` 为 `关于我们`
</acceptance_criteria>

---

### Task 3: 更新首页导航添加"关于我们"链接

<read_first>
- frontend/src/views/Home.vue — 现有 header 导航（约第 48-53 行）
</read_first>

<action>
在 `frontend/src/views/Home.vue` 的 `<template>` 部分，修改 header 导航：

将：
```vue
<nav>
  <router-link to="/login">管理登录</router-link>
</nav>
```

改为：
```vue
<nav>
  <router-link to="/about">关于我们</router-link>
  <router-link to="/login">管理登录</router-link>
</nav>
```
</action>

<acceptance_criteria>
- `frontend/src/views/Home.vue` header 导航包含"关于我们"链接
- 链接指向 `/about` 路由
</acceptance_criteria>

---

### Task 4: 更新 Home.vue 样式

<read_first>
- frontend/src/views/Home.vue — 现有 header 样式（约第 103-108 行）
</read_first>

<action>
在 `frontend/src/views/Home.vue` 的 `<style scoped>` 部分，修改 `.header nav a` 样式：

将：
```css
.header nav a {
  padding: 8px 16px;
  background: var(--color-primary);
  color: white;
  border-radius: 4px;
}
```

改为：
```css
.header nav {
  display: flex;
  gap: 12px;
}

.header nav a {
  padding: 8px 16px;
  background: var(--color-primary);
  color: white;
  border-radius: 4px;
  text-decoration: none;
  transition: opacity 0.2s;
}

.header nav a:hover {
  opacity: 0.85;
}
```
</action>

<acceptance_criteria>
- `frontend/src/views/Home.vue` 包含 `.header nav` flex 布局样式
- `.header nav a` 包含 hover 效果
</acceptance_criteria>

---

## Verification

**Manual test:**
1. 启动前后端：
   ```bash
   cd backend && npm run dev
   cd frontend && npm run dev
   ```
2. 访问首页，验证导航栏显示"关于我们"链接
3. 点击"关于我们"，验证：
   - 页面正确加载
   - 显示工作室名称和 Logo
   - 显示联系方式（电话、邮箱、地址）
   - 显示富文本介绍内容
   - 未配置时显示空状态提示
4. 验证响应式布局（缩小浏览器窗口）

**Type check:**
```bash
cd frontend && npm run type-check
```

---

## Dependencies

- 11-01: 后端 API 必须完成
- 11-02: 后台设置页面必须完成（用于配置测试数据）

---

## Risks

| Risk | Mitigation |
|------|------------|
| XSS 攻击通过 v-html | 后端已使用 sanitize-html 过滤，前端仅显示已过滤内容 |
| 样式与深色主题不兼容 | 使用 CSS 变量，确保与全局主题一致 |
| 图片加载失败 | 优雅降级，不显示图片 |

---

*Plan created: 2026-03-26*
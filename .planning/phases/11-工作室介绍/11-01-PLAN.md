---
wave: 1
depends_on: []
files_modified:
  - backend/src/services/settingsService.ts
  - backend/src/routes/settings.ts
  - frontend/src/types/settings.ts
  - frontend/src/api/settings.ts
requirements_addressed: [STUD-01]
autonomous: true
estimated_time: 15min
---

# Plan 11-01: 工作室设置模型

**Requirement:** STUD-01 — 管理员可以在后台配置工作室基本信息（名称、Logo、联系方式）

**Objective:** 扩展现有 SystemSettings key-value 模式，添加工作室信息存储和 API 端点。

---

## Context

**Why:** 工作室需要展示基本信息让访客了解。这些信息需要持久化存储并可通过后台管理。

**Key insight:** 复用现有 `SystemSettings` 模型和 `settingsService` 模式，添加 `studio_info` key，与水印配置模式一致。

---

## Tasks

### Task 1: 定义 StudioInfo 类型（后端）

<read_first>
- backend/src/services/settingsService.ts — 现有 WatermarkConfig 类型和服务模式
</read_first>

<action>
在 `backend/src/services/settingsService.ts` 中，在 `WatermarkConfig` 接口（约第 5-12 行）后添加 `StudioInfo` 类型：

```typescript
export interface StudioInfo {
  name: string;
  logo?: string;
  phone?: string;
  email?: string;
  address?: string;
  description?: string;  // HTML 富文本
}
```

在 `SettingsService` 类中添加默认配置和存取方法（约第 60 行后）：

```typescript
async getStudioInfo(): Promise<StudioInfo> {
  const settings = await this.repo.findOne({
    where: { key: 'studio_info' }
  });
  if (!settings) {
    return this.getDefaultStudioInfo();
  }
  return settings.value as StudioInfo;
}

async setStudioInfo(info: StudioInfo): Promise<void> {
  let settings = await this.repo.findOne({
    where: { key: 'studio_info' }
  });
  if (!settings) {
    settings = this.repo.create({
      key: 'studio_info',
      value: info as Record<string, unknown>
    });
  } else {
    settings.value = info as Record<string, unknown>;
  }
  await this.repo.save(settings);
}

private getDefaultStudioInfo(): StudioInfo {
  return {
    name: '',
    phone: '',
    email: '',
    address: '',
    description: ''
  };
}
```
</action>

<acceptance_criteria>
- `backend/src/services/settingsService.ts` 包含 `StudioInfo` 接口
- `backend/src/services/settingsService.ts` 包含 `getStudioInfo` 方法
- `backend/src/services/settingsService.ts` 包含 `setStudioInfo` 方法
- `backend/src/services/settingsService.ts` 包含 `getDefaultStudioInfo` 方法
</acceptance_criteria>

---

### Task 2: 安装 sanitize-html 依赖

<read_first>
- backend/package.json — 当前依赖列表
</read_first>

<action>
在 `backend` 目录下运行：

```bash
cd backend && npm install sanitize-html && npm install -D @types/sanitize-html
```

sanitize-html 用于 XSS 防护，过滤用户输入的富文本内容。
</action>

<acceptance_criteria>
- `backend/package.json` 包含 `sanitize-html` 依赖
- `backend/package.json` 包含 `@types/sanitize-html` 开发依赖
</acceptance_criteria>

---

### Task 3: 添加工作室信息 API 路由

<read_first>
- backend/src/routes/settings.ts — 现有水印配置路由模式
</read_first>

<action>
在 `backend/src/routes/settings.ts` 中：

1. 添加导入（约第 2 行后）：
```typescript
import sanitizeHtml from 'sanitize-html';
import { StudioInfo } from '../services/settingsService.js';
```

2. 配置 Logo 上传存储（在 watermarkStorage 后，约第 28 行）：
```typescript
const logoStorage = multer.diskStorage({
  destination: (req, file, cb) => {
    const dir = path.join(process.cwd(), 'uploads/logos');
    if (!fs.existsSync(dir)) {
      fs.mkdirSync(dir, { recursive: true });
    }
    cb(null, dir);
  },
  filename: (req, file, cb) => {
    const ext = path.extname(file.originalname);
    cb(null, `logo_${uuidv4()}${ext}`);
  }
});

const logoUpload = multer({ storage: logoStorage });
```

3. 添加 XSS 过滤函数：
```typescript
function sanitizeDescription(html: string): string {
  return sanitizeHtml(html, {
    allowedTags: ['p', 'br', 'strong', 'em', 'u', 'h1', 'h2', 'h3', 'ul', 'ol', 'li', 'a', 'img', 'span', 'div'],
    allowedAttributes: {
      a: ['href', 'title', 'target'],
      img: ['src', 'alt', 'title', 'width', 'height'],
      span: ['style'],
      div: ['style']
    },
    allowedSchemes: ['http', 'https', 'mailto']
  });
}
```

4. 添加 API 端点（在文件末尾 export 前）：
```typescript
router.get('/studio', async (req: Request, res: Response) => {
  try {
    const info = await settingsService.getStudioInfo();
    res.json(successResponse(info));
  } catch (error: any) {
    res.status(500).json(errorResponse(ErrorCodes.UNKNOWN, error.message));
  }
});

router.put('/studio', authMiddleware, async (req: Request, res: Response) => {
  try {
    const info: StudioInfo = req.body;

    if (!info.name || !info.name.trim()) {
      res.status(400).json(errorResponse(ErrorCodes.VALIDATION_ERROR, '工作室名称不能为空'));
      return;
    }

    if (info.description) {
      info.description = sanitizeDescription(info.description);
    }

    await settingsService.setStudioInfo(info);
    res.json(successResponse(info, '工作室信息已保存'));
  } catch (error: any) {
    res.status(500).json(errorResponse(ErrorCodes.UNKNOWN, error.message));
  }
});

router.post('/studio/logo', authMiddleware, logoUpload.single('logo'), async (req: Request, res: Response) => {
  try {
    if (!req.file) {
      res.status(400).json(errorResponse(ErrorCodes.VALIDATION_ERROR, '请上传Logo图片'));
      return;
    }

    const logoPath = `/uploads/logos/${req.file.filename}`;
    res.json(successResponse({ logoPath }, 'Logo上传成功'));
  } catch (error: any) {
    res.status(500).json(errorResponse(ErrorCodes.UNKNOWN, error.message));
  }
});
```
</action>

<acceptance_criteria>
- `backend/src/routes/settings.ts` 包含 `sanitizeHtml` 导入
- `backend/src/routes/settings.ts` 包含 `logoStorage` 和 `logoUpload` 配置
- `backend/src/routes/settings.ts` 包含 `GET /api/settings/studio` 路由
- `backend/src/routes/settings.ts` 包含 `PUT /api/settings/studio` 路由
- `backend/src/routes/settings.ts` 包含 `POST /api/settings/studio/logo` 路由
- PUT 路由包含 XSS 过滤逻辑
- PUT 路由包含名称必填验证
</acceptance_criteria>

---

### Task 4: 定义前端类型

<read_first>
- frontend/src/types/settings.ts — 现有 WatermarkConfig 类型
</read_first>

<action>
在 `frontend/src/types/settings.ts` 中添加：

```typescript
export interface StudioInfo {
  name: string;
  logo?: string;
  phone?: string;
  email?: string;
  address?: string;
  description?: string;
}
```
</action>

<acceptance_criteria>
- `frontend/src/types/settings.ts` 包含 `StudioInfo` 接口
</acceptance_criteria>

---

### Task 5: 添加前端 API 方法

<read_first>
- frontend/src/api/settings.ts — 现有 settingsApi 模式
</read_first>

<action>
在 `frontend/src/api/settings.ts` 中：

1. 添加导入：
```typescript
import type { WatermarkConfig, StudioInfo } from '@/types/settings';
```

2. 在 `settingsApi` 对象中添加方法：
```typescript
async getStudioInfo(): Promise<StudioInfo> {
  const response = await api.get<ApiResponse<StudioInfo>>('/settings/studio');
  return response.data.data;
},

async setStudioInfo(info: StudioInfo): Promise<StudioInfo> {
  const response = await api.put<ApiResponse<StudioInfo>>('/settings/studio', info);
  return response.data.data;
},

async uploadStudioLogo(file: File): Promise<{ logoPath: string }> {
  const formData = new FormData();
  formData.append('logo', file);
  const response = await api.post<ApiResponse<{ logoPath: string }>>('/settings/studio/logo', formData, {
    headers: { 'Content-Type': 'multipart/form-data' }
  });
  return response.data.data;
}
```
</action>

<acceptance_criteria>
- `frontend/src/api/settings.ts` 包含 `StudioInfo` 类型导入
- `frontend/src/api/settings.ts` 包含 `getStudioInfo` 方法
- `frontend/src/api/settings.ts` 包含 `setStudioInfo` 方法
- `frontend/src/api/settings.ts` 包含 `uploadStudioLogo` 方法
</acceptance_criteria>

---

## Verification

**Manual test:**
1. 启动后端 `cd backend && npm run dev`
2. 测试 GET 端点：
   ```bash
   curl http://localhost:3000/api/settings/studio
   ```
   应返回默认工作室信息 `{ name: '', ... }`
3. 测试 PUT 端点：
   ```bash
   curl -X PUT http://localhost:3000/api/settings/studio \
     -H "Content-Type: application/json" \
     -H "Cookie: <admin-token>" \
     -d '{"name":"测试工作室","phone":"12345678"}'
   ```
   应返回成功
4. 再次 GET 验证数据已保存

**Type check:**
```bash
cd backend && npm run build
cd frontend && npm run type-check
```

---

## Dependencies

None. 基于 existing SystemSettings 模型扩展。

---

## Risks

| Risk | Mitigation |
|------|------------|
| XSS 攻击通过富文本 | 使用 sanitize-html 过滤，仅允许安全标签 |
| Logo 文件堆积 | 可考虑定期清理未使用的 Logo 文件 |
| 前端类型不同步 | 共享类型定义，后端导出类型 |

---

*Plan created: 2026-03-26*
---
phase: 03-公开展示与私密分享
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/routes/public.ts
  - backend/src/services/publicService.ts
  - backend/src/app.ts
autonomous: true
requirements:
  - PUBL-01
  - PUBL-02
  - PUBL-03
  - PUBL-04
must_haves:
  truths:
    - "访客可以在首页看到公开作品的列表"
    - "访客可以按相册筛选作品"
    - "访客可以按标签筛选作品"
    - "访客可以搜索作品标题和描述"
    - "公开 API 不需要认证"
  artifacts:
    - path: "backend/src/routes/public.ts"
      provides: "Public API routes without auth"
      exports: ["GET /api/public/works", "GET /api/public/albums", "GET /api/public/tags"]
    - path: "backend/src/services/publicService.ts"
      provides: "Public data access with isPublic filter"
      contains: "isPublic = true"
  key_links:
    - from: "backend/src/routes/public.ts"
      to: "backend/src/services/publicService.ts"
      via: "service method calls"
      pattern: "publicService\\."
    - from: "backend/src/app.ts"
      to: "backend/src/routes/public.ts"
      via: "route registration"
      pattern: "app\\.use\\('/api/public'"
---

<objective>
Create public API routes that allow unauthenticated access to public works, albums, and tags with filtering and search capabilities.

Purpose: Enable visitors to browse public gallery content without logging in.
Output: Public API endpoints that filter by isPublic=true.
</objective>

<execution_context>
@C:/workspace/photo-show/.opencode/get-shit-done/workflows/execute-plan.md
@C:/workspace/photo-show/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-公开展示与私密分享/03-CONTEXT.md
@.planning/phases/03-公开展示与私密分享/03-RESEARCH.md

<interfaces>
<!-- Key types from existing codebase -->

From backend/src/models/Work.ts:
```typescript
@Entity('works')
export class Work {
  id: string;
  title: string;
  description: string;
  filePath: string;
  thumbnailSmall: string;
  thumbnailLarge: string;
  fileType: 'image' | 'video';
  isPublic: boolean;
  viewCount: number;
  downloadCount: number;
  albums!: Album[];
  tags!: Tag[];
}
```

From backend/src/services/workService.ts:
```typescript
async getWorks(options?: { albumId?: string; tagId?: string; isPublic?: boolean }): Promise<Work[]>
```

From backend/src/routes/works.ts:
```typescript
// Pattern: router.get('/', authMiddleware, async (req, res) => {...})
// Response: res.json(successResponse(data))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create PublicService with search and filter</name>
  <files>backend/src/services/publicService.ts</files>
  <behavior>
    - Test 1: getPublicWorks returns only isPublic=true works
    - Test 2: getPublicWorks filters by albumId correctly
    - Test 3: getPublicWorks filters by tagId correctly
    - Test 4: searchWorks returns works matching title or description
    - Test 5: getPublicWorks supports pagination (limit, offset)
  </behavior>
  <read_first>
    - backend/src/services/workService.ts (existing service pattern)
    - backend/src/models/Work.ts (Work entity structure)
    - backend/src/config/database.ts (AppDataSource export)
  </read_first>
  <action>
    Create `backend/src/services/publicService.ts` with the following methods:
    
    1. `getPublicWorks(options)`:
       - Query works with isPublic = true
       - Support filters: albumId, tagId
       - Support pagination: limit (default 20), offset
       - Include relations: albums, tags
       - Sort: isPinned DESC, position ASC
       - Return { works, total, hasMore }
    
    2. `searchPublicWorks(query, options)`:
       - Use TypeORM QueryBuilder with LIKE search
       - Search in title AND description (per D-06)
       - Query format: `%${query}%`
       - Apply isPublic = true filter
       - Support pagination
    
    3. `getPublicWorkById(id)`:
       - Get single work with relations
       - Only return if isPublic = true
       - Increment viewCount after fetching
    
    4. `getPublicAlbums()`:
       - List all albums (albums don't have isPublic, show all)
       - Include coverPath, work count
    
    5. `getPublicTags()`:
       - List all tags with work count
       - Only count public works
    
    Use existing patterns from workService.ts:
    - Use AppDataSource.getRepository(Work)
    - Use createQueryBuilder for complex queries
    - Use successResponse/errorResponse format
  </action>
  <verify>
    <automated>cd backend && npm test -- --run publicService</automated>
  </verify>
  <done>
    - publicService.ts exports PublicService class
    - All methods filter by isPublic=true
    - Search uses LIKE query on title and description
    - Pagination returns { works, total, hasMore }
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create public routes without authentication</name>
  <files>backend/src/routes/public.ts</files>
  <behavior>
    - Test 1: GET /api/public/works returns 200 without auth
    - Test 2: GET /api/public/works?albumId=xxx filters correctly
    - Test 3: GET /api/public/works?q=search returns matching works
    - Test 4: GET /api/public/works/:id returns 404 for non-public work
    - Test 5: GET /api/public/albums returns all albums
    - Test 6: GET /api/public/tags returns all tags
  </behavior>
  <read_first>
    - backend/src/routes/works.ts (existing route pattern)
    - backend/src/services/publicService.ts (created in Task 1)
    - backend/src/types/response.ts (ApiResponse type)
  </read_first>
  <action>
    Create `backend/src/routes/public.ts` with the following endpoints:
    
    1. `GET /api/public/works`:
       - Query params: albumId, tagId, q (search), limit (default 20), offset (default 0)
       - If q param exists, call searchPublicWorks
       - Otherwise call getPublicWorks
       - Return { works, total, hasMore }
    
    2. `GET /api/public/works/:id`:
       - Call getPublicWorkById
       - Return 404 if not found or not public
       - Return work with albums and tags
    
    3. `GET /api/public/albums`:
       - Return list of albums with work counts
    
    4. `GET /api/public/tags`:
       - Return list of tags with public work counts
    
    **CRITICAL: Do NOT add authMiddleware** - these routes must be accessible without authentication (per D-02).
    
    Follow existing pattern from works.ts:
    - Use Router() from express
    - Wrap in try/catch
    - Use successResponse/errorResponse
    - Return proper HTTP status codes
  </action>
  <verify>
    <automated>cd backend && npm test -- --run publicRoutes</automated>
  </verify>
  <done>
    - public.ts exports Express router
    - No authMiddleware on any route
    - All routes return proper ApiResponse format
    - Search and filter work correctly
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire public routes to app</name>
  <files>backend/src/app.ts</files>
  <read_first>
    - backend/src/app.ts (current app configuration)
    - backend/src/routes/public.ts (created in Task 2)
  </read_first>
  <action>
    Update `backend/src/app.ts`:
    
    1. Import publicRoutes:
       ```typescript
       import publicRoutes from './routes/public.js';
       ```
    
    2. Register route BEFORE existing /api routes:
       ```typescript
       app.use('/api/public', publicRoutes);
       ```
    
    Add after health check, before auth routes:
    - Line ~40: `app.use('/api/public', publicRoutes);`
    
    This ensures /api/public/* endpoints are accessible without authentication.
  </action>
  <verify>
    <automated>curl -s http://localhost:3000/api/public/works | head -c 100</automated>
  </verify>
  <done>
    - GET /api/public/works returns 200 without Authorization header
    - TypeScript compiles without errors
    - Existing routes still work
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run `npm run build` in backend - must succeed
2. Run `npm test -- --run` in backend - all tests pass
3. Test endpoints manually:
   - GET /api/public/works (no auth)
   - GET /api/public/works?albumId=xxx
   - GET /api/public/works?q=test
   - GET /api/public/albums
   - GET /api/public/tags
</verification>

<success_criteria>
- [ ] Public API endpoints accessible without authentication
- [ ] Works filtered by isPublic=true
- [ ] Search works on title and description
- [ ] Filter by albumId and tagId works
- [ ] Pagination returns correct metadata
- [ ] All tests pass
- [ ] TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-公开展示与私密分享/03-01-SUMMARY.md`
</output>
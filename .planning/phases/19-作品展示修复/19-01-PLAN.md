---
phase: 19-作品展示修复
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/gallery/WorkCard.vue
  - frontend/src/views/WorkDetail.vue
  - frontend/src/components/gallery/MediaLightbox.vue
autonomous: true
requirements: [SHOW-01, SHOW-02, SHOW-03, SHOW-04]

must_haves:
  truths:
    - "首页作品卡片正确显示作品缩略图"
    - "作品详情页网格显示所有文件，包括第一个文件"
    - "详情页大图左键切换到上一个文件，右键切换到下一个文件"
    - "进入作品详情页时，大图正确显示第一个文件"
  artifacts:
    - path: "frontend/src/components/gallery/WorkCard.vue"
      provides: "Work card component for home page"
      contains: "useWorkThumbnail, img src"
      exports: ["WorkCard component"]
    - path: "frontend/src/views/WorkDetail.vue"
      provides: "Work detail page"
      contains: "mediaItems computed, MediaLightbox, lightboxIndex"
      exports: ["WorkDetail view"]
    - path: "frontend/src/components/gallery/MediaLightbox.vue"
      provides: "Lightbox component"
      contains: "vue-easy-lightbox, click navigation, currentIndex"
      exports: ["MediaLightbox component"]
  key_links:
    - from: "WorkCard.vue"
      to: "useWorkThumbnail composable"
      via: "import"
      pattern: "thumbnailUrl"
    - from: "WorkDetail.vue"
      to: "MediaLightbox.vue"
      via: "component import"
      pattern: "MediaLightbox"
    - from: "MediaLightbox.vue"
      to: "vue-easy-lightbox"
      via: "dependency"
      pattern: "VueEasyLightbox"
---

<objective>
修复作品展示相关的问题：确保缩略图正确显示、详情页显示所有文件、灯箱导航逻辑正确

Purpose: 解决前台展示的多个问题，提升用户体验
Output: 正确显示的缩略图、完整的详情页、符合预期的灯箱导航
</objective>

<execution_context>
@C:/workspace/photo-show/.opencode/get-shit-done/workflows/execute-plan.md
@C:/workspace/photo-show/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key interfaces from codebase analysis -->

From frontend/src/components/gallery/WorkCard.vue:
```vue
<script setup lang="ts">
import { useWorkThumbnail } from '@/composables/useWorkThumbnail';

const props = defineProps<{ work: Work }>();
const { thumbnailUrl } = useWorkThumbnail(props.work);
</script>

<template>
  <div class="work-card">
    <img :src="thumbnailUrl || '/placeholder.jpg'" />
  </div>
</template>
```

From frontend/src/views/WorkDetail.vue:
```typescript
// Computed mediaItems with fallback for legacy works
const mediaItems = computed(() => {
  if (work.value?.mediaItems && work.value.mediaItems.length > 0) {
    return work.value.mediaItems;
  }
  // Fallback for legacy works without mediaItems
  if (work.value?.filePath) {
    return [{
      id: work.value.id,
      filePath: work.value.filePath,
      thumbnailSmall: work.value.thumbnailSmall,
      thumbnailLarge: work.value.thumbnailLarge,
      // ... other fields
    }];
  }
  return [];
});

// Lightbox state
const lightboxVisible = ref(false);
const lightboxIndex = ref(0);

const openLightbox = (index: number) => {
  lightboxIndex.value = index;
  lightboxVisible.value = true;
};
```

From frontend/src/components/gallery/MediaLightbox.vue:
```vue
<script setup lang="ts">
import VueEasyLightbox from 'vue-easy-lightbox';

// Current implementation uses vue-easy-lightbox
// Default click behavior: click on image to zoom, not for navigation
// Need to add custom click handlers for left/right navigation
</script>
```
</interfaces>
</context>

<analysis>
<!-- Problem Analysis -->

## Current Issues

**SHOW-01: 首页作品缩略图正确显示**
- Status: Already implemented via useWorkThumbnail composable
- WorkCard.vue uses thumbnailUrl from useWorkThumbnail
- useWorkThumbnail prioritizes mediaItems[0].thumbnailLarge
- Fallback to work.thumbnailLarge for legacy data
- Conclusion: Likely already working, need to verify

**SHOW-02: 作品详情页正确显示所有文件（包括第一个文件）**
- Status: Already implemented in WorkDetail.vue
- mediaItems computed handles both new and legacy data
- Grid displays all mediaItems
- Conclusion: Likely already working, need to verify

**SHOW-03: 详情页大图左右切换逻辑正确**
- Status: NOT implemented correctly
- Current: vue-easy-lightbox has default click behavior (zoom on click)
- Required: Left click → previous file, Right click → next file
- Need to: Customize click handlers, disable default zoom on click
- This is the main issue to fix

**SHOW-04: 进入详情页时大图正确显示**
- Status: Already implemented
- lightboxIndex is set correctly when opening lightbox
- vue-easy-lightbox respects the initial index
- Conclusion: Likely already working, need to verify

## Solution Approach

1. Verify SHOW-01, SHOW-02, SHOW-04 are working (no changes needed)
2. Fix SHOW-03 by customizing MediaLightbox click handlers:
   - Add left/right click handlers on image
   - Prevent default behavior
   - Call onPrev/onNext accordingly
   - Ensure this doesn't interfere with zoom functionality
</analysis>

<tasks>
<!-- Task definitions -->

## Task 1: Verify thumbnail display (SHOW-01)

**Goal:** Ensure WorkCard displays thumbnails correctly

**Actions:**
1. Review WorkCard.vue implementation
2. Test with works that have mediaItems
3. Test with legacy works (no mediaItems)
4. Verify thumbnailUrl is computed correctly

**Verification:**
- Thumbnail displays for works with mediaItems
- Thumbnail displays for legacy works
- No broken images

**Done criteria:**
- All tests pass
- No changes needed if already working

---

## Task 2: Verify detail page file display (SHOW-02)

**Goal:** Ensure WorkDetail displays all files including first

**Actions:**
1. Review WorkDetail.vue mediaItems computed
2. Test with works that have multiple mediaItems
3. Verify grid displays all items
4. Verify no items are missing

**Verification:**
- All mediaItems display in grid
- First file is visible
- No missing items

**Done criteria:**
- All tests pass
- No changes needed if already working

---

## Task 3: Fix lightbox click navigation (SHOW-03)

**Goal:** Implement left/right click navigation in lightbox

**Actions:**
1. Research vue-easy-lightbox click customization options
2. Add click event handlers to lightbox image
3. Implement left click → previous, right click → next
4. Ensure zoom still works (double-click or scroll)
5. Test navigation with multiple images

**Implementation:**
```vue
<template>
  <VueEasyLightbox
    :visible="visible"
    :imgs="imageUrls"
    :index="currentIndex"
    @click="handleClick"
  >
    <!-- Custom click overlay -->
  </VueEasyLightbox>
</template>

<script setup lang="ts">
const handleClick = (e: MouseEvent) => {
  // Check which mouse button was clicked
  if (e.button === 0) { // Left click
    onPrev();
  } else if (e.button === 2) { // Right click
    onNext();
  }
};

// Prevent context menu on right click
const onContextMenu = (e: Event) => {
  e.preventDefault();
};
</script>
```

**Verification:**
- Left click navigates to previous image
- Right click navigates to next image
- Zoom still works (double-click or scroll)
- Keyboard navigation still works
- Context menu is suppressed

**Done criteria:**
- Click navigation works as expected
- No conflicts with existing functionality

---

## Task 4: Verify initial lightbox display (SHOW-04)

**Goal:** Ensure lightbox displays correct initial image

**Actions:**
1. Review lightboxIndex handling in WorkDetail.vue
2. Test opening lightbox from different grid items
3. Verify correct image displays initially

**Verification:**
- Lightbox opens with correct image
- Initial index matches clicked grid item
- No off-by-one errors

**Done criteria:**
- All tests pass
- No changes needed if already working

</tasks>

<verification>
<!-- Overall verification steps -->

## Success Criteria

1. **SHOW-01:** Home page work cards display thumbnails correctly
   - Test: Navigate to home page
   - Verify: All work cards show thumbnails
   - Check: Both new (mediaItems) and legacy works

2. **SHOW-02:** Detail page displays all files including first
   - Test: Click on work card to open detail page
   - Verify: All mediaItems display in grid
   - Check: First file is visible

3. **SHOW-03:** Lightbox click navigation works correctly
   - Test: Open lightbox from detail page
   - Verify: Left click navigates to previous
   - Verify: Right click navigates to next
   - Check: Zoom still works (double-click)

4. **SHOW-04:** Initial lightbox image is correct
   - Test: Click different grid items
   - Verify: Lightbox opens with correct image
   - Check: Index matches clicked item

## Manual Testing Steps

1. Start dev server: `cd frontend && npm run dev`
2. Navigate to home page
3. Verify all work cards show thumbnails
4. Click a work card to open detail page
5. Verify all files display in grid
6. Click a grid item to open lightbox
7. Test left click navigation
8. Test right click navigation
9. Test keyboard navigation (arrow keys)
10. Test zoom (double-click or scroll)
11. Close lightbox and click different grid items
12. Verify initial image is correct each time

</verification>

<success_criteria>
<!-- What must be TRUE after completion -->

1. 首页作品卡片正确显示作品缩略图
2. 作品详情页网格显示所有文件，包括第一个文件
3. 详情页大图左键切换到上一个文件，右键切换到下一个文件
4. 进入作品详情页时，大图正确显示第一个文件

</success_criteria>

<output_spec>
<!-- Expected outputs -->

- Updated MediaLightbox.vue with custom click handlers
- All SHOW requirements verified and passing
- No regressions in existing functionality
- 19-01-SUMMARY.md documenting all changes and verifications

</output_spec>
---
phase: 05-部署与优化
plan: 05-01
wave: 1
autonomous: true
requirements: []
created: 2026-03-25
status: pending
---

# Plan: PM2 Process Manager Setup

## Objective

Configure PM2 for production Node.js process management with cluster mode.

## Tasks

### Task 1: Create PM2 Ecosystem File

Create `ecosystem.config.js`:

```javascript
module.exports = {
  apps: [{
    name: 'photo-show-api',
    script: 'dist/index.js',
    cwd: '/var/www/photo-show/backend',
    instances: 'max',
    exec_mode: 'cluster',
    autorestart: true,
    watch: false,
    max_memory_restart: '500M',
    env_production: {
      NODE_ENV: 'production',
      PORT: 3000
    },
    error_file: '/var/log/photo-show/error.log',
    out_file: '/var/log/photo-show/out.log',
    log_date_format: 'YYYY-MM-DD HH:mm:ss Z',
    merge_logs: true,
    log_rotate: true,
    log_rotate_size: '10M',
    log_rotate_count: 30
  }]
}
```

### Task 2: Create PM2 Startup Script

Create `deploy/pm2-setup.sh`:

```bash
#!/bin/bash
# Install PM2 globally
npm install -g pm2

# Create log directory
sudo mkdir -p /var/log/photo-show
sudo chown $USER:$USER /var/log/photo-show

# Start application
pm2 start ecosystem.config.js --env production

# Save PM2 process list
pm2 save

# Generate startup script
pm2 startup
```

### Task 3: Create Process Management Commands

Create `deploy/pm2-commands.sh`:

```bash
#!/bin/bash
# Common PM2 commands for this project

case "$1" in
  start)
    pm2 start ecosystem.config.js --env production
    ;;
  stop)
    pm2 stop photo-show-api
    ;;
  restart)
    pm2 restart photo-show-api
    ;;
  reload)
    pm2 reload photo-show-api
    ;;
  logs)
    pm2 logs photo-show-api
    ;;
  status)
    pm2 status
    ;;
  monit)
    pm2 monit
    ;;
  *)
    echo "Usage: $0 {start|stop|restart|reload|logs|status|monit}"
    exit 1
esac
```

## Files Modified

- `ecosystem.config.js` (new)
- `deploy/pm2-setup.sh` (new)
- `deploy/pm2-commands.sh` (new)

## Dependencies

- None (first plan in phase)

## Success Criteria

- [ ] PM2 ecosystem config created
- [ ] Setup script executable
- [ ] Process management commands documented
- [ ] Ready for deployment testing
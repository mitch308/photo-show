---
phase: 17-样式统一
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/views/admin/Shares.vue
  - frontend/src/views/admin/Clients.vue
autonomous: true
requirements: [AUX-07]
user_setup: []

must_haves:
  truths:
    - "分享管理页面表格样式与作品管理页面一致"
    - "客户管理页面表格样式与作品管理页面一致"
    - "所有后台管理页面使用统一的 Element Plus 组件"
  artifacts:
    - path: "frontend/src/views/admin/Shares.vue"
      provides: "Element Plus styled shares management"
      contains: "el-table"
    - path: "frontend/src/views/admin/Clients.vue"
      provides: "Element Plus styled clients management"
      contains: "el-table"
  key_links:
    - from: "Shares.vue"
      to: "Element Plus"
      via: "el-table, el-dialog, el-form"
      pattern: "el-"
    - from: "Clients.vue"
      to: "Element Plus"
      via: "el-table, el-dialog, el-form"
      pattern: "el-"
---

<objective>
Convert Shares.vue and Clients.vue from raw HTML elements to Element Plus components for visual consistency across all admin pages.

Purpose: Provide a unified visual experience in the admin panel, eliminating jarring style differences between pages.
Output: Both pages using el-table, el-dialog, el-form, el-button, and other Element Plus components consistent with Works.vue and Albums.vue.
</objective>

<execution_context>
@C:/workspace/photo-show/.opencode/get-shit-done/workflows/execute-plan.md
@C:/workspace/photo-show/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Reference: Element Plus components used in Works.vue and Albums.vue -->

From frontend/src/views/admin/Works.vue:
```vue
<!-- Table pattern -->
<el-table :data="worksStore.works" v-loading="worksStore.loading" stripe>
  <el-table-column prop="title" label="标题" min-width="150" />
  <el-table-column label="状态" width="120">
    <template #default="{ row }">
      <el-tag :type="row.isPublic ? 'success' : 'info'">
        {{ row.isPublic ? '公开' : '私密' }}
      </el-tag>
    </template>
  </el-table-column>
  <el-table-column label="操作" width="150" fixed="right">
    <template #default="{ row }">
      <el-button link type="primary" @click="openEditDialog(row)">编辑</el-button>
      <el-button link type="danger" @click="deleteWork(row)">删除</el-button>
    </template>
  </el-table-column>
</el-table>

<!-- Dialog pattern -->
<el-dialog v-model="dialogVisible" :title="editingWork ? '编辑作品' : '上传作品'" width="600px">
  <el-form :model="form" label-width="80px">
    <el-form-item label="标题" required>
      <el-input v-model="form.title" placeholder="请输入标题" />
    </el-form-item>
  </el-form>
  <template #footer>
    <el-button @click="dialogVisible = false">取消</el-button>
    <el-button type="primary" @click="saveWork">保存</el-button>
  </template>
</el-dialog>

<!-- Filter pattern -->
<el-input v-model="searchQuery" placeholder="搜索作品标题..." clearable style="width: 200px" />
<el-select v-model="statusFilter" placeholder="状态筛选" clearable style="width: 120px">
  <el-option label="公开" value="public" />
  <el-option label="私密" value="private" />
</el-select>
```

From frontend/src/views/admin/Albums.vue:
```vue
<!-- Simple table pattern -->
<el-table :data="albums" v-loading="loading" stripe>
  <el-table-column prop="name" label="名称" min-width="150" />
  <el-table-column label="操作" width="200">
    <template #default="{ row }">
      <el-button link type="primary" @click="openEditDialog(row)">编辑</el-button>
      <el-button link type="danger" @click="deleteAlbum(row)">删除</el-button>
    </template>
  </el-table-column>
</el-table>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert Shares.vue to Element Plus components</name>
  <files>frontend/src/views/admin/Shares.vue</files>
  <action>
Replace all raw HTML elements with Element Plus components to match Works.vue style:

1. **Imports**: Add `import { ElMessage, ElMessageBox } from 'element-plus';`

2. **Table conversion** (lines 170-216):
   - Replace `<table>` with `<el-table :data="shares" v-loading="loading" stripe>`
   - Replace `<thead>/<tr>/<th>` with `<el-table-column prop="..." label="..." />`
   - Replace `<tbody>/<tr>/<td>` with template slots in el-table-column
   - Replace custom `.status` spans with `<el-tag :type="...">`
   - Replace custom `.type-tag` spans with `<el-tag>`

3. **Filter selects** (lines 152-162):
   - Replace `<select v-model="clientFilter">` with `<el-select v-model="clientFilter">`
   - Replace `<option>` with `<el-option>`
   - Add `clearable` and `placeholder` props

4. **Buttons** (line 164, 206-212):
   - Replace `<button class="btn-primary">` with `<el-button type="primary">`
   - Replace `<button>` with `<el-button>`
   - Add `link` type for table action buttons

5. **Dialogs** (lines 224-279, 282-315):
   - Replace `.dialog-overlay/.dialog` with `<el-dialog v-model="...">`
   - Replace `<div class="form-group">` with `<el-form>` + `<el-form-item>`
   - Replace `<input>`, `<select>` with `<el-input>`, `<el-select>`
   - Use `<template #footer>` for dialog actions

6. **Remove custom CSS**: Delete all the custom table, dialog, button, status styles that are now handled by Element Plus

7. **Replace alerts**: Replace `alert(...)` with `ElMessage.success/error(...)`
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | grep -E "(error|Error)" || echo "Build successful"</automated>
  </verify>
  <done>
    - Shares.vue uses el-table, el-dialog, el-form, el-button, el-select, el-input
    - No raw HTML table, select, button, input elements
    - Custom dialog-overlay/dialog CSS removed
    - Visual style matches Works.vue and Albums.vue
  </done>
</task>

<task type="auto">
  <name>Task 2: Convert Clients.vue to Element Plus components</name>
  <files>frontend/src/views/admin/Clients.vue</files>
  <action>
Replace all raw HTML elements with Element Plus components to match Works.vue style:

1. **Imports**: Add `import { ElMessage, ElMessageBox } from 'element-plus';`

2. **Table conversion** (lines 195-222):
   - Replace `<table>` with `<el-table :data="clients" v-loading="loading" stripe>`
   - Replace `<thead>/<tr>/<th>` with `<el-table-column prop="..." label="..." />`
   - Replace `<tbody>/<tr>/<td>` with template slots in el-table-column

3. **Search input** (line 182-186):
   - Replace `<input type="text" class="search-input">` with `<el-input v-model="searchQuery" placeholder="..." clearable>`

4. **Buttons** (line 188, 216-218):
   - Replace `<button class="btn-primary">` with `<el-button type="primary">`
   - Replace `<button>` with `<el-button link type="...">` for table actions

5. **Dialogs** (lines 234-443):
   - Replace all `.dialog-overlay/.dialog` with `<el-dialog v-model="...">`
   - Replace `<div class="form-group">` with `<el-form>` + `<el-form-item label-width="80px">`
   - Replace `<input>`, `<textarea>`, `<select>` with `<el-input>`, `<el-input type="textarea">`, `<el-select>`
   - Use `<template #footer>` for dialog actions

6. **Detail dialog** (lines 332-407):
   - Convert to el-dialog with el-descriptions for client info display
   - Convert embedded shares table to el-table

7. **Remove custom CSS**: Delete all the custom table, dialog, button, status styles

8. **Replace alerts/confirms**: Replace `alert(...)` with `ElMessage.success/error(...)`, replace `confirm(...)` with `ElMessageBox.confirm(...)`
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | grep -E "(error|Error)" || echo "Build successful"</automated>
  </verify>
  <done>
    - Clients.vue uses el-table, el-dialog, el-form, el-button, el-input
    - No raw HTML table, select, button, input elements
    - Custom dialog-overlay/dialog CSS removed
    - Visual style matches Works.vue and Albums.vue
  </done>
</task>

</tasks>

<verification>
1. Build completes without errors
2. Shares.vue renders with el-table showing all columns
3. Clients.vue renders with el-table showing all columns
4. All dialogs use el-dialog component
5. Filter dropdowns use el-select
6. Status badges use el-tag
7. All buttons use el-button
</verification>

<success_criteria>
1. 分享管理页面使用 el-table 展示分享列表，表格样式与作品管理页面一致
2. 客户管理页面使用 el-table 展示客户列表，表格样式与作品管理页面一致
3. 两个页面均使用 el-dialog 替代自定义弹窗
4. 两个页面均使用 el-form + el-form-item 替代自定义表单
5. 所有后台管理页面视觉风格统一
</success_criteria>

<output>
After completion, create `.planning/phases/17-样式统一/17-01-SUMMARY.md`
</output>
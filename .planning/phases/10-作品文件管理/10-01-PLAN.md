---
wave: 1
depends_on: []
files_modified:
  - frontend/src/views/admin/Works.vue
requirements_addressed: [WORK-03]
autonomous: true
estimated_time: 10min
---

# Plan 10-01: 添加文件功能

**Requirement:** WORK-03 — 管理员可以为已有作品添加新文件

**Objective:** 在编辑作品对话框中添加文件管理区域，让管理员可以为已有作品上传新的图片或视频文件。

---

## Context

**Why:** 作品创建后，管理员可能需要追加更多文件（如同一拍摄的多张照片）。目前只能通过删除作品重建来添加文件，操作不便。

**Key insight:** 后端 API 已完整实现（`POST /works/:workId/media`），前端只需在编辑对话框中复用现有 Upload 组件，上传成功后调用 `mediaItemsApi.addMediaItem()`。

---

## Tasks

### Task 1: 导入 mediaItemsApi 和 MediaItem 类型

<read_first>
- frontend/src/views/admin/Works.vue — 当前 imports（约第 1-10 行）
- frontend/src/api/mediaItems.ts:18 — addMediaItem 函数签名
</read_first>

<action>
在 `frontend/src/views/admin/Works.vue` 的 `<script setup>` 部分，修改 imports（约第 1-10 行）：

将：
```typescript
import { ref, onMounted } from 'vue';
import { ElMessage, ElMessageBox } from 'element-plus';
import { useWorksStore } from '@/stores/works';
import { albumsApi } from '@/api/albums';
import { tagsApi } from '@/api/tags';
import { batchApi } from '@/api/batch';
import Upload from '@/components/Upload.vue';
import BatchActionBar from '@/components/BatchActionBar.vue';
import type { Album, Tag, UploadResult, Work } from '@/api/types';
```

改为：
```typescript
import { ref, onMounted } from 'vue';
import { ElMessage, ElMessageBox } from 'element-plus';
import { useWorksStore } from '@/stores/works';
import { albumsApi } from '@/api/albums';
import { tagsApi } from '@/api/tags';
import { batchApi } from '@/api/batch';
import { mediaItemsApi } from '@/api/mediaItems';
import Upload from '@/components/Upload.vue';
import BatchActionBar from '@/components/BatchActionBar.vue';
import type { Album, Tag, UploadResult, Work, MediaItem } from '@/api/types';
```

变更：添加 `mediaItemsApi` 导入和 `MediaItem` 类型导入。
</action>

<acceptance_criteria>
- `frontend/src/views/admin/Works.vue` 包含 `import { mediaItemsApi } from '@/api/mediaItems'`
- `frontend/src/views/admin/Works.vue` 包含 `import type { ..., MediaItem } from '@/api/types'`
</acceptance_criteria>

---

### Task 2: 添加文件上传状态变量

<read_first>
- frontend/src/views/admin/Works.vue — 当前状态变量（约第 16-33 行）
</read_first>

<action>
在 `frontend/src/views/admin/Works.vue` 的 `<script setup>` 部分，在 `uploadedFile` 变量（约第 27 行）后添加新状态变量：

```typescript
const showAddFile = ref(false);
const addingFile = ref(false);
```

说明：
- `showAddFile`: 控制添加文件区域的显示
- `addingFile`: 文件添加中的加载状态
</action>

<acceptance_criteria>
- `frontend/src/views/admin/Works.vue` 包含 `const showAddFile = ref(false)`
- `frontend/src/views/admin/Works.vue` 包含 `const addingFile = ref(false)`
</acceptance_criteria>

---

### Task 3: 添加文件处理函数

<read_first>
- frontend/src/views/admin/Works.vue — 当前的 saveWork/deleteWork 函数（约第 76-109 行）
- frontend/src/api/mediaItems.ts:19 — addMediaItem 函数签名
</read_first>

<action>
在 `frontend/src/views/admin/Works.vue` 的 `<script setup>` 部分，在 `deleteWork` 函数（约第 97-109 行）后添加文件处理函数：

```typescript
async function handleAddFile(result: UploadResult & { isDuplicate?: boolean }) {
  if (!editingWork.value) return;
  
  addingFile.value = true;
  try {
    const formData = new FormData();
    formData.append('filePath', result.filePath);
    formData.append('thumbnailSmall', result.thumbnailSmall || '');
    formData.append('thumbnailLarge', result.thumbnailLarge || '');
    formData.append('originalFilename', result.originalFilename);
    formData.append('fileType', result.fileType);
    formData.append('mimeType', result.mimeType);
    formData.append('fileSize', String(result.fileSize));
    if (result.fileHash) {
      formData.append('fileHash', result.fileHash);
    }
    
    await mediaItemsApi.addMediaItem(editingWork.value.id, formData);
    
    ElMessage.success('文件添加成功');
    showAddFile.value = false;
    
    await worksStore.fetchWorks();
    
    const updated = worksStore.works.find(w => w.id === editingWork.value?.id);
    if (updated) {
      editingWork.value = updated;
    }
  } catch (error: any) {
    ElMessage.error(error.message || '添加失败');
  } finally {
    addingFile.value = false;
  }
}

function cancelAddFile() {
  showAddFile.value = false;
}
```

说明：
- 构造 FormData 包含所有必需字段
- 调用 mediaItemsApi.addMediaItem 添加文件
- 刷新作品列表并更新当前编辑的作品
</action>

<acceptance_criteria>
- `frontend/src/views/admin/Works.vue` 包含 `async function handleAddFile(result: UploadResult & { isDuplicate?: boolean })`
- 函数包含 `await mediaItemsApi.addMediaItem(editingWork.value.id, formData)` 调用
- 函数包含 `await worksStore.fetchWorks()` 调用
- 函数包含 `editingWork.value = updated` 更新逻辑
</acceptance_criteria>

---

### Task 4: 在编辑对话框中添加文件管理区域

<read_first>
- frontend/src/views/admin/Works.vue — 当前的编辑对话框（约第 228-285 行）
</read_first>

<action>
在 `frontend/src/views/admin/Works.vue` 的 `<template>` 部分，修改编辑对话框内容（约第 239-277 行）：

在 `</el-form-item>` （isPublic 那项，约第 275 行）后、`</el-form>` 前，插入文件管理区域：

```vue
        <!-- 文件管理区域（仅编辑模式） -->
        <el-form-item v-if="editingWork" label="文件">
          <div class="files-manager">
            <div class="files-list">
              <div 
                v-for="item in editingWork.mediaItems" 
                :key="item.id" 
                class="file-item"
              >
                <el-image
                  v-if="item.thumbnailSmall"
                  :src="`/${item.thumbnailSmall}`"
                  fit="cover"
                  class="file-thumb"
                />
                <div v-else class="file-thumb file-thumb-placeholder">
                  {{ item.fileType === 'video' ? '🎬' : '📷' }}
                </div>
                <span class="file-name">{{ item.originalFilename }}</span>
              </div>
            </div>
            
            <el-button 
              v-if="!showAddFile"
              size="small" 
              @click="showAddFile = true"
            >
              添加文件
            </el-button>
            
            <div v-else class="add-file-area">
              <Upload @success="handleAddFile" />
              <el-button 
                size="small" 
                @click="cancelAddFile" 
                style="margin-top: 10px"
              >
                取消
              </el-button>
            </div>
          </div>
        </el-form-item>
```

完整结构：在 isPublic 的 el-form-item 后添加此文件管理区域。
</action>

<acceptance_criteria>
- `frontend/src/views/admin/Works.vue` 包含 `<el-form-item v-if="editingWork" label="文件">`
- 该区域包含 `v-for="item in editingWork.mediaItems"` 循环渲染文件列表
- 该区域包含 Upload 组件用于添加文件
- Upload 组件绑定 `@success="handleAddFile"` 事件
</acceptance_criteria>

---

### Task 5: 添加文件管理区域样式

<read_first>
- frontend/src/views/admin/Works.vue — 当前样式（约第 412-433 行）
</read_first>

<action>
在 `frontend/src/views/admin/Works.vue` 的 `<style scoped>` 部分（约第 432 行后）添加文件管理区域样式：

```css
.files-manager {
  width: 100%;
}

.files-list {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  margin-bottom: 10px;
}

.file-item {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 8px;
  background: var(--el-fill-color-light);
  border-radius: 4px;
  min-width: 200px;
}

.file-thumb {
  width: 40px;
  height: 40px;
  border-radius: 4px;
  object-fit: cover;
  flex-shrink: 0;
}

.file-thumb-placeholder {
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--el-fill-color);
  font-size: 18px;
}

.file-name {
  font-size: 13px;
  color: var(--el-text-color-regular);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  flex: 1;
}

.add-file-area {
  margin-top: 10px;
}
```
</action>

<acceptance_criteria>
- `frontend/src/views/admin/Works.vue` 包含 `.files-manager` CSS 类
- `frontend/src/views/admin/Works.vue` 包含 `.files-list` CSS 类
- `frontend/src/views/admin/Works.vue` 包含 `.file-item` CSS 类
- `frontend/src/views/admin/Works.vue` 包含 `.file-thumb` CSS 类
</acceptance_criteria>

---

## Verification

**Manual test:**
1. 启动前端 `cd frontend && npm run dev`
2. 登录后台，进入作品管理页面
3. 点击任意作品的"编辑"按钮
4. 验证：
   - 编辑对话框显示"文件"区域
   - 文件区域显示当前作品的所有文件（缩略图 + 文件名）
   - 点击"添加文件"按钮，显示上传区域
   - 上传新文件后，文件列表自动刷新
   - 作品列表中的文件数量正确更新

**Type check:**
```bash
cd frontend && npm run type-check
```

---

## Dependencies

None. 后端 API 已实现，纯前端工作。

---

## Risks

| Risk | Mitigation |
|------|------------|
| FormData 字段遗漏 | 参考 backend/src/routes/mediaItems.ts:39 的字段要求 |
| 上传区域遮挡编辑表单 | 使用条件渲染，添加/取消切换清晰 |
| 刷新后编辑对话框关闭 | 保存 editingWork.id，刷新后重新定位 |

---

*Plan created: 2026-03-26*
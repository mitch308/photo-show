---
phase: 13-bug-修复
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/composables/useWorkThumbnail.ts
  - frontend/src/components/gallery/WorkCard.vue
autonomous: true
requirements:
  - BUG-02
must_haves:
  truths:
    - "作品缩略图正确显示（使用第一个 mediaItem，兼容旧数据）"
    - "无缩略图时显示占位图，不会显示损坏图片"
  artifacts:
    - path: "frontend/src/composables/useWorkThumbnail.ts"
      provides: "缩略图获取逻辑，支持 mediaItems 和旧数据兼容"
      min_lines: 20
      exports: ["useWorkThumbnail"]
    - path: "frontend/src/components/gallery/WorkCard.vue"
      provides: "作品卡片显示，使用缩略图 composable"
      contains: "useWorkThumbnail"
  key_links:
    - from: "frontend/src/components/gallery/WorkCard.vue"
      to: "frontend/src/composables/useWorkThumbnail.ts"
      via: "import { useWorkThumbnail }"
      pattern: "useWorkThumbnail"
    - from: "useWorkThumbnail"
      to: "Work type"
      via: "mediaItems[0].thumbnailLarge"
      pattern: "mediaItems\\[0\\]"
---

<objective>
创建 useWorkThumbnail composable 并更新 WorkCard 组件，确保作品缩略图正确显示。

Purpose: 修复缩略图显示问题，支持新的 mediaItems 数据结构，同时兼容旧数据。
Output: useWorkThumbnail composable 和更新后的 WorkCard.vue。
</objective>

<execution_context>
@.opencode/get-shit-done/workflows/execute-plan.md
@.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-bug-修复/13-CONTEXT.md

## Data Model Context

### Work Type (from frontend/src/api/types.ts)
```typescript
export interface Work {
  id: string;
  title: string;
  // Legacy single-file fields (deprecated, use mediaItems instead)
  thumbnailLarge?: string | null;
  // New multi-media support
  mediaItems?: MediaItem[];
  // ... other fields
}

export interface MediaItem {
  id: string;
  thumbnailLarge?: string | null;
  // ... other fields
}
```

### Current Issue (WorkCard.vue line 15)
```vue
<img :src="`/${work.thumbnailLarge}`" />
```
- 直接使用 `work.thumbnailLarge`，不支持 `mediaItems[0].thumbnailLarge`
- 新数据结构下缩略图存储在 `mediaItems[0]`

### Compatibility Requirements
1. 优先使用 `work.mediaItems[0].thumbnailLarge`（新数据）
2. 降级使用 `work.thumbnailLarge`（旧数据兼容）
3. 处理空数据情况（显示占位图）
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 useWorkThumbnail composable</name>
  <files>frontend/src/composables/useWorkThumbnail.ts</files>
  <read_first>
    - frontend/src/api/types.ts — 理解 Work 和 MediaItem 类型定义
    - frontend/src/composables/useTheme.ts — 参考现有 composable 模式
  </read_first>
  <behavior>
    - Test 1: work 有 mediaItems[0].thumbnailLarge 时返回该值
    - Test 2: work 无 mediaItems 但有 thumbnailLarge 时返回 thumbnailLarge（旧数据兼容）
    - Test 3: work 无缩略图时返回 null
    - Test 4: work.mediaItems 为空数组时降级到 thumbnailLarge
  </behavior>
  <action>
    创建 `frontend/src/composables/useWorkThumbnail.ts`：

    ```typescript
    import { computed } from 'vue';
    import type { Work } from '@/api/types';

    /**
     * 获取作品缩略图 URL
     * 优先使用 mediaItems[0].thumbnailLarge，兼容旧数据的 work.thumbnailLarge
     *
     * @param work 作品对象
     * @returns 缩略图 URL，如果没有则返回 null
     */
    export function useWorkThumbnail(work: Work) {
      const thumbnailUrl = computed(() => {
        // 1. 优先使用 mediaItems[0].thumbnailLarge（新数据结构）
        if (work.mediaItems && work.mediaItems.length > 0 && work.mediaItems[0].thumbnailLarge) {
          return work.mediaItems[0].thumbnailLarge;
        }

        // 2. 降级使用 work.thumbnailLarge（旧数据兼容）
        if (work.thumbnailLarge) {
          return work.thumbnailLarge;
        }

        // 3. 无缩略图
        return null;
      });

      return {
        thumbnailUrl,
      };
    }
    ```

    实现要点：
    - 使用 computed 确保响应式
    - 先检查 mediaItems 存在且非空
    - 降级到 work.thumbnailLarge 兼容旧数据
    - 返回 null 供调用方处理占位图
  </action>
  <verify>
    <automated>grep -n "useWorkThumbnail" frontend/src/composables/useWorkThumbnail.ts && echo "PASS: Composable exports useWorkThumbnail"</automated>
    <automated>grep -n "mediaItems\[0\]" frontend/src/composables/useWorkThumbnail.ts && echo "PASS: Uses mediaItems[0]"</automated>
    <automated>grep -n "thumbnailLarge" frontend/src/composables/useWorkThumbnail.ts && echo "PASS: Has fallback for old data"</automated>
  </verify>
  <done>
    - useWorkThumbnail composable 已创建
    - 支持 mediaItems[0].thumbnailLarge 优先
    - 兼容 work.thumbnailLarge 旧数据
    - 无缩略图时返回 null
  </done>
</task>

<task type="auto">
  <name>Task 2: 更新 WorkCard.vue 使用 composable</name>
  <files>frontend/src/components/gallery/WorkCard.vue</files>
  <read_first>
    - frontend/src/components/gallery/WorkCard.vue — 当前实现，直接使用 work.thumbnailLarge
    - frontend/src/composables/useWorkThumbnail.ts — 新创建的 composable
  </read_first>
  <action>
    更新 `frontend/src/components/gallery/WorkCard.vue`：

    1. **导入 composable**：
       ```typescript
       import { useWorkThumbnail } from '@/composables/useWorkThumbnail';
       ```

    2. **使用 composable 获取缩略图**：
       ```typescript
       const props = defineProps<{
         work: Work;
       }>();

       const { thumbnailUrl } = useWorkThumbnail(props.work);
       ```

    3. **更新模板使用计算后的 URL**：
       - 将 `:src="\`/${work.thumbnailLarge}\`"` 改为 `:src="thumbnailUrl ? \`/${thumbnailUrl}\` : undefined"`
       - 添加占位图处理：无缩略图时显示默认占位

    4. **占位图实现**：
       - 使用 CSS 背景或 Element Plus 的 el-image 占位
       - 或显示简单的灰色背景 + 图标

    完整更新后的 script 部分：
    ```vue
    <script setup lang="ts">
    import { ref } from 'vue';
    import type { Work } from '@/api/types';
    import { useWorkThumbnail } from '@/composables/useWorkThumbnail';

    const props = defineProps<{
      work: Work;
    }>();

    const { thumbnailUrl } = useWorkThumbnail(props.work);
    const imageLoaded = ref(false);
    </script>
    ```

    模板更新：
    ```vue
    <template>
      <div class="work-card">
        <img
          v-if="thumbnailUrl"
          :src="`/${thumbnailUrl}`"
          :alt="work.title"
          loading="lazy"
          @load="imageLoaded = true"
          :class="{ loaded: imageLoaded }"
        />
        <div v-else class="placeholder">
          <span>无图片</span>
        </div>
        <div class="work-overlay">
          <h3>{{ work.title }}</h3>
        </div>
      </div>
    </template>
    ```

    添加占位图样式：
    ```css
    .placeholder {
      width: 100%;
      aspect-ratio: 1;
      display: flex;
      align-items: center;
      justify-content: center;
      background: var(--bg-card, #f0f0f0);
      color: var(--text-secondary, #999);
      font-size: 14px;
    }
    ```
  </action>
  <verify>
    <automated>grep -n "useWorkThumbnail" frontend/src/components/gallery/WorkCard.vue && echo "PASS: WorkCard imports useWorkThumbnail"</automated>
    <automated>grep -n "thumbnailUrl" frontend/src/components/gallery/WorkCard.vue && echo "PASS: WorkCard uses thumbnailUrl"</automated>
    <automated>grep -n "placeholder" frontend/src/components/gallery/WorkCard.vue && echo "PASS: Has placeholder for missing thumbnail"</automated>
  </verify>
  <done>
    - WorkCard.vue 已导入 useWorkThumbnail
    - 缩略图使用 thumbnailUrl（支持新数据结构）
    - 无缩略图时显示占位图
    - 保持原有样式和交互
  </done>
</task>

</tasks>

<verification>
## BUG-02 验证清单

- [ ] useWorkThumbnail composable 已创建
- [ ] composable 支持 mediaItems[0].thumbnailLarge
- [ ] composable 兼容 work.thumbnailLarge 旧数据
- [ ] WorkCard.vue 使用 useWorkThumbnail
- [ ] 无缩略图时显示占位图而非损坏图片
- [ ] 保持原有样式和交互效果
</verification>

<success_criteria>
- 作品缩略图正确显示（新数据使用 mediaItems[0]）
- 旧数据兼容（降级使用 work.thumbnailLarge）
- 无缩略图时显示占位图，不显示损坏图片
- 原有卡片样式和悬停效果保持不变
</success_criteria>

<output>
After completion, create `.planning/phases/13-bug-修复/13-02-SUMMARY.md`
</output>
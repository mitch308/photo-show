---
phase: 08-file-storage-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/models/MediaItem.ts
  - backend/src/services/uploadService.ts
  - backend/src/middlewares/upload.ts
  - backend/src/routes/upload.ts
  - backend/src/routes/mediaItems.ts
  - frontend/src/components/Upload.vue
  - frontend/src/api/types.ts
  - frontend/src/utils/hash.ts
  - frontend/package.json
  - backend/package.json
autonomous: true
requirements:
  - FILE-01
user_setup: []

must_haves:
  truths:
    - "前端上传前计算 fast-md5 并显示计算进度"
    - "调用 GET /api/media/check?hash=xxx 检查文件是否已存在"
    - "已存在文件返回已有信息，跳过上传"
    - "新文件以 fast-md5 作为文件名存储"
    - "MediaItem 包含 fileHash 字段"
  artifacts:
    - path: "backend/src/models/MediaItem.ts"
      provides: "fileHash 字段存储 fast-md5"
      contains: "fileHash"
    - path: "backend/src/routes/mediaItems.ts"
      provides: "预检查 API 端点"
      exports: ["GET /check"]
    - path: "frontend/src/utils/hash.ts"
      provides: "Fast-MD5 计算工具函数"
      exports: ["computeFastMd5"]
  key_links:
    - from: "frontend/src/components/Upload.vue"
      to: "/api/media/check"
      via: "axios GET with hash param"
      pattern: "computeFastMd5.*check"
    - from: "backend/src/routes/mediaItems.ts"
      to: "MediaItem repository"
      via: "findOneBy fileHash"
      pattern: "fileHash.*findOne"
---

<objective>
实现 Fast-MD5 预检查去重：上传前计算文件哈希，检查是否已存在，存在则跳过上传。

Purpose: 节省带宽和存储空间，避免重复文件存储
Output: Fast-MD5 去重系统，前端计算哈希，后端检查和存储

</objective>

<execution_context>
@C:/workspace/photo-show/.opencode/get-shit-done/workflows/execute-plan.md
@C:/workspace/photo-show/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-file-storage-optimization/08-CONTEXT.md

<interfaces>
<!-- Key types and contracts executor needs -->

From backend/src/models/MediaItem.ts:
```typescript
@Entity('media_items')
export class MediaItem {
  @PrimaryColumn({ type: 'varchar', length: 36 })
  id: string = '';

  @Column({ type: 'varchar', length: 36, name: 'work_id' })
  workId: string = '';

  @Column({ type: 'varchar', length: 500, name: 'file_path' })
  filePath: string = '';

  @Column({ type: 'varchar', length: 500, nullable: true, name: 'thumbnail_small' })
  thumbnailSmall: string = '';

  @Column({ type: 'varchar', length: 500, nullable: true, name: 'thumbnail_large' })
  thumbnailLarge: string = '';

  @Column({ type: 'varchar', length: 255, name: 'original_filename' })
  originalFilename: string = '';

  @Column({ type: 'varchar', length: 50, name: 'file_type' })
  fileType: 'image' | 'video' = 'image';

  @Column({ type: 'varchar', length: 50, name: 'mime_type' })
  mimeType: string = '';

  @Column({ type: 'int', default: 0, name: 'file_size' })
  fileSize: number = 0;
}
```

From frontend/src/api/types.ts:
```typescript
export interface UploadResult {
  filePath: string;
  thumbnailSmall: string | null;
  thumbnailLarge: string | null;
  originalFilename: string;
  fileType: 'image' | 'video';
  mimeType: string;
  fileSize: number;
}

export interface MediaItem {
  id: string;
  workId: string;
  filePath: string;
  thumbnailSmall?: string | null;
  thumbnailLarge?: string | null;
  originalFilename: string;
  fileType: 'image' | 'video';
  mimeType: string;
  fileSize: number;
  position: number;
  createdAt: string;
  updatedAt: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 后端 - 添加 fileHash 字段和预检查 API</name>
  <files>
    backend/src/models/MediaItem.ts
    backend/src/routes/mediaItems.ts
    backend/src/services/mediaItemService.ts
    backend/src/migrations/run-migration.ts
  </files>
  <read_first>
    - backend/src/models/MediaItem.ts
    - backend/src/services/mediaItemService.ts
    - backend/src/routes/mediaItems.ts
  </read_first>
  <action>
    1. **修改 MediaItem 模型** (`backend/src/models/MediaItem.ts`):
       - 添加 `fileHash` 字段：`@Column({ type: 'varchar', length: 32, unique: true, nullable: true, name: 'file_hash' }) fileHash: string = '';`
       - 字段长度 32（MD5 输出长度），nullable: true 允许现有数据迁移

    2. **添加数据库迁移** (`backend/src/migrations/run-migration.ts`):
       - 添加 `file_hash` 列到 `media_items` 表
       - SQL: `ALTER TABLE media_items ADD COLUMN file_hash VARCHAR(32) NULL UNIQUE`

    3. **修改 mediaItemService** (`backend/src/services/mediaItemService.ts`):
       - 添加 `findByHash(hash: string)` 方法
       - 使用 `repository.findOneBy({ fileHash: hash })`

    4. **添加预检查 API** (`backend/src/routes/mediaItems.ts`):
       - 新增 GET `/check` 端点
       - 接受 query 参数 `hash`
       - 调用 `mediaItemService.findByHash(hash)`
       - 响应格式：
         ```json
         { "success": true, "data": { "exists": boolean, "mediaItem": MediaItem | null } }
         ```
       - 此端点需要 `authMiddleware` 保护

    5. **更新前端类型** (`frontend/src/api/types.ts`):
       - UploadResult 添加 `fileHash?: string`
       - MediaItem 添加 `fileHash?: string`
       - 添加 `MediaCheckResult { exists: boolean; mediaItem?: MediaItem }`
  </action>
  <acceptance_criteria>
    - `backend/src/models/MediaItem.ts` 包含 `@Column` 装饰的 `fileHash` 字段
    - `backend/src/services/mediaItemService.ts` 包含 `findByHash` 方法
    - `backend/src/routes/mediaItems.ts` 包含 `GET /check` 路由
    - `frontend/src/api/types.ts` 包含 `MediaCheckResult` 接口
  </acceptance_criteria>
  <verify>
    <automated>grep -n "fileHash" backend/src/models/MediaItem.ts && grep -n "findByHash" backend/src/services/mediaItemService.ts && grep -n "check" backend/src/routes/mediaItems.ts</automated>
  </verify>
  <done>
    - MediaItem 模型有 fileHash 字段
    - 预检查 API GET /api/media/check?hash=xxx 可用
    - 前端类型定义已更新
  </done>
</task>

<task type="auto">
  <name>Task 2: 后端 - 修改上传流程使用 hash 命名</name>
  <files>
    backend/src/middlewares/upload.ts
    backend/src/services/uploadService.ts
    backend/src/routes/upload.ts
  </files>
  <read_first>
    - backend/src/middlewares/upload.ts
    - backend/src/services/uploadService.ts
    - backend/src/routes/upload.ts
    - backend/src/config/storage.ts
  </read_first>
  <action>
    1. **修改 upload 中间件** (`backend/src/middlewares/upload.ts`):
       - 保持现有的 Multer diskStorage 配置
       - 文件名生成逻辑改为：如果请求中有 `fileHash` 字段（来自 body 或 query），使用 `{fileHash}.{ext}` 作为文件名
       - 否则回退到 UUID 命名（向后兼容）
       - 修改 `filename` 回调：
         ```typescript
         filename: (req, file, cb) => {
           const ext = path.extname(file.originalname);
           const hash = req.body.fileHash || req.query.fileHash;
           if (hash) {
             cb(null, `${hash}${ext}`);
           } else {
             const uuid = uuidv4();
             cb(null, `${uuid}${ext}`);
           }
         }
         ```

    2. **修改 uploadService** (`backend/src/services/uploadService.ts`):
       - `processFile` 方法接受可选的 `fileHash` 参数
       - 如果提供 fileHash，验证计算结果是否匹配（可选安全检查）
       - 在 UploadResult 中返回 fileHash
       - 缩略图命名改为 `{fileHash}_small.{ext}` 和 `{fileHash}_large.{ext}`

    3. **修改 upload 路由** (`backend/src/routes/upload.ts`):
       - 从请求 body 或 query 中提取 `fileHash`
       - 传递给 `uploadService.processFile`
       - 响应中包含 fileHash
  </action>
  <acceptance_criteria>
    - `backend/src/middlewares/upload.ts` 的 `filename` 回调检查 `req.body.fileHash` 或 `req.query.fileHash`
    - 文件以 `{hash}.{ext}` 格式存储
    - `backend/src/services/uploadService.ts` 返回 `fileHash` 字段
  </acceptance_criteria>
  <verify>
    <automated>grep -n "req.body.fileHash\|req.query.fileHash" backend/src/middlewares/upload.ts && grep -n "fileHash" backend/src/services/uploadService.ts</automated>
  </verify>
  <done>
    - 上传的文件以 fast-md5 哈希命名
    - 缩略图以 `{hash}_small.{ext}` 和 `{hash}_large.{ext}` 命名
    - UploadResult 包含 fileHash
  </done>
</task>

<task type="auto">
  <name>Task 3: 前端 - 安装 SparkMD5 并实现 hash 计算</name>
  <files>
    frontend/package.json
    frontend/src/utils/hash.ts
    frontend/src/api/mediaItems.ts
  </files>
  <read_first>
    - frontend/package.json
    - frontend/src/components/Upload.vue
    - frontend/src/api/types.ts
  </read_first>
  <action>
    1. **安装 SparkMD5**:
       ```bash
       cd frontend && npm install spark-md5 && npm install -D @types/spark-md5
       ```

    2. **创建 hash 工具函数** (`frontend/src/utils/hash.ts`):
       ```typescript
       import SparkMD5 from 'spark-md5';

       /**
        * 计算 Fast-MD5: MD5(file_size + first_64KB_content)
        * @param file 要计算哈希的文件
        * @param onProgress 进度回调 (0-100)
        */
       export async function computeFastMd5(
         file: File,
         onProgress?: (percent: number) => void
       ): Promise<string> {
         const fileSize = file.size;
         const chunkSize = Math.min(64 * 1024, fileSize); // 64KB 或文件大小
         
         onProgress?.(0);
         
         // 读取前 64KB
         const chunk = file.slice(0, chunkSize);
         const chunkBuffer = await chunk.arrayBuffer();
         
         onProgress?.(50);
         
         // 组合: file_size (8 bytes little-endian) + first_64KB
         const sizeBuffer = new ArrayBuffer(8);
         new DataView(sizeBuffer).setBigUint64(0, BigInt(fileSize), true);
         
         const combined = new Uint8Array(8 + chunkBuffer.byteLength);
         combined.set(new Uint8Array(sizeBuffer), 0);
         combined.set(new Uint8Array(chunkBuffer), 8);
         
         // 计算 MD5
         const spark = new SparkMD5.ArrayBuffer();
         spark.append(combined);
         
         onProgress?.(100);
         
         return spark.end();
       }
       ```

    3. **添加 API 方法** (`frontend/src/api/mediaItems.ts` 或新建):
       - 添加 `checkFileHash(hash: string)` 方法
       - 调用 GET `/api/media/check?hash=${hash}`
       - 返回 `MediaCheckResult`

    4. **更新类型定义** (`frontend/src/api/types.ts`):
       - 已在 Task 1 完成
  </action>
  <acceptance_criteria>
    - `frontend/package.json` 包含 `spark-md5` 依赖
    - `frontend/src/utils/hash.ts` 导出 `computeFastMd5` 函数
    - API 方法 `checkFileHash` 可用
  </acceptance_criteria>
  <verify>
    <automated>grep -n "spark-md5" frontend/package.json && test -f frontend/src/utils/hash.ts && grep -n "computeFastMd5" frontend/src/utils/hash.ts</automated>
  </verify>
  <done>
    - SparkMD5 已安装
    - computeFastMd5 函数可用
    - checkFileHash API 方法可用
  </done>
</task>

<task type="auto">
  <name>Task 4: 前端 - 修改上传组件实现预检查</name>
  <files>
    frontend/src/components/Upload.vue
  </files>
  <read_first>
    - frontend/src/components/Upload.vue
    - frontend/src/utils/hash.ts (from Task 3)
    - frontend/src/api/types.ts
  </read_first>
  <action>
    修改 `frontend/src/components/Upload.vue`:

    1. **导入依赖**:
       ```typescript
       import { computeFastMd5 } from '@/utils/hash';
       import { checkFileHash } from '@/api/mediaItems';
       ```

    2. **添加状态**:
       ```typescript
       const computingHash = ref(false);
       const hashProgress = ref(0);
       const checkingHash = ref(false);
       ```

    3. **修改 handleUpload 函数**:
       ```typescript
       async function handleUpload(file: File) {
         // Step 1: 计算 Fast-MD5
         computingHash.value = true;
         hashProgress.value = 0;
         
         try {
           const hash = await computeFastMd5(file, (p) => {
             hashProgress.value = p;
           });
           computingHash.value = false;
           
           // Step 2: 预检查
           checkingHash.value = true;
           const checkResult = await checkFileHash(hash);
           checkingHash.value = false;
           
           if (checkResult.exists && checkResult.mediaItem) {
             // 文件已存在，跳过上传
             emit('success', {
               ...checkResult.mediaItem,
               isDuplicate: true, // 标记为重复文件
             });
             ElMessage.success('文件已存在，已跳过上传');
             return;
           }
           
           // Step 3: 上传新文件
           uploading.value = true;
           uploadProgress.value = 0;
           
           const formData = new FormData();
           formData.append('file', file);
           formData.append('fileHash', hash);
           
           const isVideo = file.type.startsWith('video/');
           const endpoint = isVideo ? '/upload/video' : '/upload/image';
           
           const response = await api.post(endpoint, formData, {
             headers: { 'Content-Type': 'multipart/form-data' },
             onUploadProgress: (e) => {
               if (e.total) {
                 uploadProgress.value = Math.round((e.loaded * 100) / e.total);
               }
             },
           });
           
           const result: UploadResult = response.data.data;
           emit('success', result);
           ElMessage.success('上传成功');
         } catch (error: any) {
           emit('error', error);
           ElMessage.error(error.message || '上传失败');
         } finally {
           computingHash.value = false;
           checkingHash.value = false;
           uploading.value = false;
           uploadProgress.value = 0;
           hashProgress.value = 0;
         }
       }
       ```

    4. **更新模板显示哈希计算进度**:
       ```vue
       <template v-if="computingHash">
         <div class="upload-progress">
           <el-progress type="circle" :percentage="hashProgress" />
           <p>计算文件哈希...</p>
         </div>
       </template>
       <template v-else-if="checkingHash">
         <div class="upload-progress">
           <el-icon class="is-loading" size="48"><Loading /></el-icon>
           <p>检查文件...</p>
         </div>
       </template>
       <template v-else-if="uploading">
         <!-- existing upload progress -->
       </template>
       ```
  </action>
  <acceptance_criteria>
    - `Upload.vue` 导入 `computeFastMd5` 和 `checkFileHash`
    - `handleUpload` 函数先计算 hash，再调用 checkFileHash
    - 文件已存在时跳过上传并显示提示
    - 新文件上传时携带 `fileHash` 字段
    - UI 显示哈希计算进度
  </acceptance_criteria>
  <verify>
    <automated>grep -n "computeFastMd5" frontend/src/components/Upload.vue && grep -n "checkFileHash" frontend/src/components/Upload.vue && grep -n "fileHash" frontend/src/components/Upload.vue</automated>
  </verify>
  <done>
    - 上传前计算 fast-md5
    - 预检查 API 检查文件是否存在
    - 存在则跳过上传，不存在则上传并携带 hash
    - UI 显示各阶段进度
  </done>
</task>

</tasks>

<verification>
1. 后端单元测试：
   - 测试 `findByHash` 方法
   - 测试 `/api/media/check` 端点

2. 集成测试：
   - 上传新文件：文件以 hash 命名
   - 上传已存在文件：返回已有记录，不重复存储
   - 验证数据库 MediaItem.fileHash 字段

3. 手动测试：
   - 上传相同文件两次，验证第二次跳过上传
   - 检查存储目录只有一个文件
</verification>

<success_criteria>
- [ ] MediaItem 模型有 fileHash 字段（32字符唯一索引）
- [ ] GET /api/media/check?hash=xxx 返回正确的存在检查结果
- [ ] 前端 computeFastMd5 正确计算 MD5(file_size + first_64KB)
- [ ] 上传流程：计算hash → 检查 → 上传或跳过
- [ ] 文件以 {hash}.{ext} 格式存储
- [ ] 重复文件跳过上传，返回已有记录
</success_criteria>

<output>
After completion, create `.planning/phases/08-file-storage-optimization/08-01-SUMMARY.md`
</output>
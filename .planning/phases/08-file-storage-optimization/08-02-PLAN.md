---
phase: 08-file-storage-optimization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/services/imageService.ts
  - backend/src/services/uploadService.ts
  - backend/src/services/mediaItemService.ts
autonomous: true
requirements:
  - FILE-02
user_setup: []

must_haves:
  truths:
    - "图片尺寸小于缩略图尺寸时不生成缩略图"
    - "访问小图片的缩略图返回原图路径"
    - "缩略图生成前检查原图尺寸"
  artifacts:
    - path: "backend/src/services/imageService.ts"
      provides: "智能缩略图生成逻辑"
      contains: "width.*<=.*300"
    - path: "backend/src/services/uploadService.ts"
      provides: "使用智能缩略图生成"
      pattern: "generateSmartThumbnails|shouldGenerateThumbnail"
  key_links:
    - from: "backend/src/services/uploadService.ts"
      to: "imageService.generateSmartThumbnails"
      via: "method call"
      pattern: "generateSmartThumbnails"
    - from: "backend/src/services/imageService.ts"
      to: "sharp.metadata"
      via: "获取图片尺寸"
      pattern: "metadata.*width"
---

<objective>
实现智能缩略图生成：检查图片尺寸，小图片不生成缩略图，直接返回原图路径。

Purpose: 避免对小图片生成无意义的缩略图，节省存储空间
Output: 智能缩略图生成逻辑，小图片返回原图路径

</objective>

<execution_context>
@C:/workspace/photo-show/.opencode/get-shit-done/workflows/execute-plan.md
@C:/workspace/photo-show/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-file-storage-optimization/08-CONTEXT.md

<interfaces>
<!-- Key types and contracts executor needs -->

From backend/src/services/imageService.ts:
```typescript
export interface ThumbnailResult {
  small: string;   // 300px
  large: string;   // 1200px
}

export class ImageService {
  async generateThumbnails(
    inputPath: string,
    outputDir: string,
    originalName: string
  ): Promise<ThumbnailResult> {
    // Currently generates both thumbnails unconditionally
  }
}
```

From backend/src/services/uploadService.ts:
```typescript
export interface UploadResult {
  filePath: string;
  thumbnailSmall: string | null;
  thumbnailLarge: string | null;
  originalFilename: string;
  fileType: 'image' | 'video';
  mimeType: string;
  fileSize: number;
}
```

缩略图尺寸定义 (per D-05):
- small: 300px 宽度
- large: 1200px 宽度
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 ThumbnailResult 支持智能返回</name>
  <files>
    backend/src/services/imageService.ts
  </files>
  <read_first>
    - backend/src/services/imageService.ts
    - backend/src/services/uploadService.ts
  </read_first>
  <action>
    修改 `backend/src/services/imageService.ts`:

    1. **扩展 ThumbnailResult 接口**:
       ```typescript
       export interface ThumbnailResult {
         small: string;           // 缩略图路径或原图路径
         large: string;           // 缩略图路径或原图路径
         smallIsOriginal: boolean; // small 是否为原图
         largeIsOriginal: boolean; // large 是否为原图
       }
       ```

    2. **添加缩略图尺寸常量**:
       ```typescript
       export const THUMBNAIL_SIZES = {
         SMALL: 300,
         LARGE: 1200,
       };
       ```

    3. **添加判断是否需要生成缩略图的方法**:
       ```typescript
       /**
        * 检查是否需要生成指定尺寸的缩略图
        * @param imageWidth 原图宽度
        * @param targetSize 目标缩略图尺寸
        * @returns true 表示需要生成，false 表示可以使用原图
        */
       shouldGenerateThumbnail(imageWidth: number, targetSize: number): boolean {
         return imageWidth > targetSize;
       }
       ```

    4. **重命名并增强 generateThumbnails 方法**（保持向后兼容）:
       ```typescript
       /**
        * 智能生成缩略图
        * - 图片宽度 <= 目标尺寸时不生成，返回原图路径
        * - 图片宽度 > 目标尺寸时生成缩略图
        */
       async generateSmartThumbnails(
         inputPath: string,
         outputDir: string,
         originalName: string,
         fileHash?: string  // 可选的 hash，用于命名
       ): Promise<ThumbnailResult> {
         const ext = path.extname(originalName);
         const uuid = fileHash || uuidv4();
         
         // 获取原图尺寸
         const image = sharp(inputPath);
         const metadata = await image.metadata();
         const width = metadata.width || 0;
         
         const result: ThumbnailResult = {
           small: '',
           large: '',
           smallIsOriginal: false,
           largeIsOriginal: false,
         };
         
         // Small thumbnail (300px)
         if (this.shouldGenerateThumbnail(width, THUMBNAIL_SIZES.SMALL)) {
           const smallPath = path.join(outputDir, `${uuid}_small${ext}`);
           await sharp(inputPath)
             .resize(THUMBNAIL_SIZES.SMALL, undefined, { fit: 'inside' })
             .toFile(smallPath);
           result.small = smallPath;
           result.smallIsOriginal = false;
         } else {
           // 图片太小，使用原图
           result.small = inputPath;
           result.smallIsOriginal = true;
         }
         
         // Large thumbnail (1200px)
         if (this.shouldGenerateThumbnail(width, THUMBNAIL_SIZES.LARGE)) {
           const largePath = path.join(outputDir, `${uuid}_large${ext}`);
           await sharp(inputPath)
             .resize(THUMBNAIL_SIZES.LARGE, undefined, { fit: 'inside' })
             .toFile(largePath);
           result.large = largePath;
           result.largeIsOriginal = false;
         } else {
           // 图片太小，使用原图
           result.large = inputPath;
           result.largeIsOriginal = true;
         }
         
         return result;
       }
       ```

    5. **保留原方法作为别名（向后兼容）**:
       ```typescript
       async generateThumbnails(
         inputPath: string,
         outputDir: string,
         originalName: string
       ): Promise<{ small: string; large: string }> {
         const result = await this.generateSmartThumbnails(inputPath, outputDir, originalName);
         return {
           small: result.small,
           large: result.large,
         };
       }
       ```
  </action>
  <acceptance_criteria>
    - `imageService.ts` 包含 `THUMBNAIL_SIZES` 常量
    - `ThumbnailResult` 接口包含 `smallIsOriginal` 和 `largeIsOriginal` 字段
    - `shouldGenerateThumbnail` 方法存在
    - `generateSmartThumbnails` 方法检查图片尺寸并智能决定是否生成缩略图
  </acceptance_criteria>
  <verify>
    <automated>grep -n "THUMBNAIL_SIZES" backend/src/services/imageService.ts && grep -n "smallIsOriginal\|largeIsOriginal" backend/src/services/imageService.ts && grep -n "shouldGenerateThumbnail" backend/src/services/imageService.ts</automated>
  </verify>
  <done>
    - ThumbnailResult 支持标识是否为原图
    - shouldGenerateThumbnail 方法可判断是否需要生成缩略图
    - generateSmartThumbnails 实现智能生成逻辑
  </done>
</task>

<task type="auto">
  <name>Task 2: 修改 uploadService 使用智能缩略图</name>
  <files>
    backend/src/services/uploadService.ts
  </files>
  <read_first>
    - backend/src/services/uploadService.ts
    - backend/src/services/imageService.ts (from Task 1)
  </read_first>
  <action>
    修改 `backend/src/services/uploadService.ts`:

    1. **修改 processImage 方法**:
       ```typescript
       async processImage(file: Express.Multer.File, fileHash?: string): Promise<UploadResult> {
         const { dir, monthDir } = getUploadPath();
         
         // 使用智能缩略图生成
         const thumbnails = await imageService.generateSmartThumbnails(
           file.path,
           dir,
           file.originalname,
           fileHash
         );
         
         // 获取水印配置
         const watermarkConfig = await settingsService.getWatermarkConfig();
         
         // 处理 small 缩略图的水印（如果不是原图）
         let thumbnailSmallPath: string;
         if (thumbnails.smallIsOriginal) {
           // 小图片，不生成缩略图，但可能需要添加水印
           thumbnailSmallPath = thumbnails.small;
           // 注意：原图不添加水印，只在缩略图上添加
           // 如果原图被用作缩略图，这里跳过水印以保护原图
         } else {
           thumbnailSmallPath = thumbnails.small;
           // 添加水印逻辑（保持现有逻辑）
           if (watermarkConfig.enabled) {
             // ... existing watermark logic for small
           }
         }
         
         // 处理 large 缩略图的水印（如果不是原图）
         let thumbnailLargePath: string | null;
         if (thumbnails.largeIsOriginal) {
           // 小图片，使用原图，跳过水印以保护原图
           thumbnailLargePath = null; // 大缩略图和原图相同时设为 null
         } else {
           thumbnailLargePath = thumbnails.large;
           // 添加水印逻辑（保持现有逻辑）
           if (watermarkConfig.enabled) {
             // ... existing watermark logic for large
           }
         }
         
         // 构建返回路径
         const filePath = fileHash 
           ? toUrlPath('uploads/works', monthDir, `${fileHash}${path.extname(file.originalname)}`)
           : toUrlPath('uploads/works', monthDir, file.filename);
         
         return {
           filePath,
           thumbnailSmall: toUrlPath('uploads/works', monthDir, path.basename(thumbnailSmallPath)),
           thumbnailLarge: thumbnailLargePath 
             ? toUrlPath('uploads/works', monthDir, path.basename(thumbnailLargePath))
             : null,
           originalFilename: file.originalname,
           fileType: 'image',
           mimeType: file.mimetype,
           fileSize: file.size,
           fileHash,
         };
       }
       ```

    2. **关键逻辑**:
       - 当 `smallIsOriginal` 为 true 时，`thumbnailSmall` 路径指向原图
       - 当 `largeIsOriginal` 为 true 时，`thumbnailLarge` 设为 null（避免冗余存储相同路径）
       - 原图不添加水印（水印只在缩略图上）
       - 如果图片宽度 <= 300px，则 thumbnailSmall 和 thumbnailLarge 都指向原图或为 null

    3. **更新 processFile 方法签名**:
       ```typescript
       async processFile(file: Express.Multer.File, fileHash?: string): Promise<UploadResult>
       ```
  </action>
  <acceptance_criteria>
    - `processImage` 调用 `generateSmartThumbnails` 而非 `generateThumbnails`
    - 当图片宽度 <= 300px 时，`thumbnailLarge` 返回 null
    - 当图片宽度 <= 1200px 时，`thumbnailLarge` 返回 null 或原图路径
    - 原图不被添加水印
  </acceptance_criteria>
  <verify>
    <automated>grep -n "generateSmartThumbnails" backend/src/services/uploadService.ts && grep -n "smallIsOriginal\|largeIsOriginal" backend/src/services/uploadService.ts</automated>
  </verify>
  <done>
    - uploadService 使用智能缩略图生成
    - 小图片不生成缩略图
    - 原图保持无水印
  </done>
</task>

<task type="auto">
  <name>Task 3: 添加单元测试验证智能缩略图逻辑</name>
  <files>
    backend/src/services/imageService.test.ts
  </files>
  <read_first>
    - backend/src/services/imageService.ts
    - backend/src/services/imageService.test.ts (if exists)
  </read_first>
  <action>
    创建或更新 `backend/src/services/imageService.test.ts`:

    ```typescript
    import { describe, it, expect, beforeAll, afterAll } from 'vitest';
    import sharp from 'sharp';
    import fs from 'fs';
    import path from 'path';
    import { imageService, THUMBNAIL_SIZES } from './imageService.js';

    describe('ImageService - Smart Thumbnails', () => {
      const testDir = path.join(process.cwd(), 'test-uploads');
      const smallImagePath = path.join(testDir, 'small.png');
      const mediumImagePath = path.join(testDir, 'medium.png');
      const largeImagePath = path.join(testDir, 'large.png');

      beforeAll(async () => {
        if (!fs.existsSync(testDir)) {
          fs.mkdirSync(testDir, { recursive: true });
        }

        // Create test images of different sizes
        await sharp({ create: { width: 200, height: 200, channels: 3, background: 'red' } })
          .png()
          .toFile(smallImagePath);

        await sharp({ create: { width: 600, height: 600, channels: 3, background: 'green' } })
          .png()
          .toFile(mediumImagePath);

        await sharp({ create: { width: 1500, height: 1500, channels: 3, background: 'blue' } })
          .png()
          .toFile(largeImagePath);
      });

      afterAll(() => {
        // Cleanup test files
        fs.rmSync(testDir, { recursive: true, force: true });
      });

      it('should return original for small images (width <= 300)', async () => {
        const result = await imageService.generateSmartThumbnails(
          smallImagePath,
          testDir,
          'small.png'
        );

        expect(result.smallIsOriginal).toBe(true);
        expect(result.largeIsOriginal).toBe(true);
        expect(result.small).toBe(smallImagePath);
        expect(result.large).toBe(smallImagePath);
      });

      it('should generate small thumbnail for medium images (300 < width <= 1200)', async () => {
        const result = await imageService.generateSmartThumbnails(
          mediumImagePath,
          testDir,
          'medium.png'
        );

        expect(result.smallIsOriginal).toBe(false);
        expect(result.largeIsOriginal).toBe(true);
        expect(result.small).not.toBe(mediumImagePath);
        expect(result.large).toBe(mediumImagePath);
      });

      it('should generate both thumbnails for large images (width > 1200)', async () => {
        const result = await imageService.generateSmartThumbnails(
          largeImagePath,
          testDir,
          'large.png'
        );

        expect(result.smallIsOriginal).toBe(false);
        expect(result.largeIsOriginal).toBe(false);
        expect(result.small).not.toBe(largeImagePath);
        expect(result.large).not.toBe(largeImagePath);
      });

      it('shouldGenerateThumbnail returns correct values', () => {
        expect(imageService.shouldGenerateThumbnail(200, THUMBNAIL_SIZES.SMALL)).toBe(false);
        expect(imageService.shouldGenerateThumbnail(400, THUMBNAIL_SIZES.SMALL)).toBe(true);
        expect(imageService.shouldGenerateThumbnail(1000, THUMBNAIL_SIZES.LARGE)).toBe(false);
        expect(imageService.shouldGenerateThumbnail(1500, THUMBNAIL_SIZES.LARGE)).toBe(true);
      });
    });
    ```
  </action>
  <acceptance_criteria>
    - 测试文件存在并可执行
    - 测试覆盖三种情况：小图片、中等图片、大图片
    - 所有测试通过
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npm test -- --filter=imageService.test.ts 2>&1 | grep -E "(PASS|FAIL|✓|✗)" | head -20</automated>
  </verify>
  <done>
    - 单元测试覆盖智能缩略图逻辑
    - 测试验证三种尺寸图片的处理
    - 所有测试通过
  </done>
</task>

</tasks>

<verification>
1. 单元测试：
   - 测试 shouldGenerateThumbnail 方法
   - 测试不同尺寸图片的缩略图生成逻辑
   - 测试小图片返回原图路径

2. 手动测试：
   - 上传 200px 宽度的图片：验证不生成缩略图
   - 上传 600px 宽度的图片：验证只生成 small 缩略图
   - 上传 1500px 宽度的图片：验证生成两个缩略图
   - 检查数据库 MediaItem 记录的 thumbnailSmall/thumbnailLarge 字段
</verification>

<success_criteria>
- [ ] imageService.generateSmartThumbnails 检查图片尺寸
- [ ] 图片宽度 <= 300px 时不生成缩略图
- [ ] 图片宽度 <= 1200px 时不生成 large 缩略图
- [ ] ThumbnailResult 包含 smallIsOriginal/largeIsOriginal 标识
- [ ] uploadService 使用智能缩略图生成
- [ ] 原图不添加水印
- [ ] 单元测试覆盖关键逻辑
</success_criteria>

<output>
After completion, create `.planning/phases/08-file-storage-optimization/08-02-SUMMARY.md`
</output>
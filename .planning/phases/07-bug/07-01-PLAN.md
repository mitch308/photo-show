---
phase: 07-bug
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/models/SystemSettings.ts
  - backend/src/models/index.ts
  - backend/src/services/uploadService.ts
  - backend/src/services/settingsService.ts
  - backend/src/routes/settings.ts
  - backend/src/app.ts
  - frontend/src/api/settings.ts
  - frontend/src/views/admin/Settings.vue
  - frontend/src/router/index.ts
autonomous: true
requirements:
  - BUG-01

must_haves:
  truths:
    - "管理员可以在后台配置水印（文字或图片、位置、透明度）"
    - "配置保存后，新上传的图片缩略图自动添加水印"
    - "公开展示使用带水印的缩略图，私密下载返回无水印原图"
  artifacts:
    - path: "backend/src/models/SystemSettings.ts"
      provides: "系统配置数据模型"
      exports: ["SystemSettings"]
    - path: "backend/src/services/settingsService.ts"
      provides: "配置存取服务"
      exports: ["settingsService"]
    - path: "backend/src/services/uploadService.ts"
      provides: "水印集成到上传流程"
      contains: "addWatermark"
    - path: "backend/src/routes/settings.ts"
      provides: "配置 API 端点"
      exports: ["GET /api/settings/watermark", "PUT /api/settings/watermark"]
  key_links:
    - from: "backend/src/services/uploadService.ts"
      to: "imageService.addWatermark()"
      via: "processImage 方法调用"
      pattern: "addWatermark"
    - from: "backend/src/routes/settings.ts"
      to: "settingsService"
      via: "API 处理函数"
      pattern: "settingsService\\."
---

<objective>
实现水印功能集成：管理员配置水印后，公开展示的作品图片显示水印。

**Purpose:** 保护摄影师作品，防止未授权使用。

**Output:**
- SystemSettings 模型存储水印配置
- 上传流程集成水印生成
- 后台管理界面配置水印
</objective>

<execution_context>
@C:/workspace/photo-show/.opencode/get-shit-done/workflows/execute-plan.md
@C:/workspace/photo-show/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-bug/07-CONTEXT.md
@.planning/research/SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From backend/src/services/imageService.ts:
```typescript
export interface WatermarkOptions {
  text?: string;
  imagePath?: string;
  position: 'top-left' | 'top-right' | 'bottom-left' | 'bottom-right' | 'center';
  opacity: number; // 0-1
}

export class ImageService {
  async addWatermark(
    inputPath: string,
    outputPath: string,
    options: WatermarkOptions
  ): Promise<string>
}
```

From backend/src/services/uploadService.ts:
```typescript
export interface UploadResult {
  filePath: string;
  thumbnailSmall: string | null;
  thumbnailLarge: string | null;
  originalFilename: string;
  fileType: 'image' | 'video';
  mimeType: string;
  fileSize: number;
}

export class UploadService {
  async processImage(file: Express.Multer.File): Promise<UploadResult>
}
```

From backend/src/models/index.ts:
```typescript
export * from './Admin.js';
export * from './Album.js';
export * from './Client.js';
export * from './MediaItem.js';
export * from './ShareAccessLog.js';
export * from './Tag.js';
export * from './Work.js';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 SystemSettings 模型</name>
  <files>backend/src/models/SystemSettings.ts, backend/src/models/index.ts</files>
  <read_first>
    - backend/src/models/Work.ts (了解现有模型模式)
    - backend/src/models/index.ts (了解导出模式)
    - backend/src/services/imageService.ts (了解 WatermarkOptions 接口)
  </read_first>
  <action>
    创建 SystemSettings 模型存储水印配置。

    1. 创建 `backend/src/models/SystemSettings.ts`:
       - 使用单例模式（id 固定为 'default'）
       - 字段 `key: string` - 配置类型（'watermark'）
       - 字段 `value: json` - JSON 存储配置内容
       - 字段 `updatedAt: Date`
       - 水印配置 JSON 结构：
         ```typescript
         {
           enabled: boolean;
           type: 'text' | 'image';
           text?: string;
           imagePath?: string;
           position: 'top-left' | 'top-right' | 'bottom-left' | 'bottom-right' | 'center';
           opacity: number; // 0-1
         }
         ```

    2. 在 `backend/src/models/index.ts` 添加导出：
       ```typescript
       export * from './SystemSettings.js';
       ```
  </action>
  <acceptance_criteria>
    - 文件 `backend/src/models/SystemSettings.ts` 存在
    - 包含 @Entity('system_settings') 装饰器
    - 包含 key, value, updatedAt 字段
    - 在 index.ts 中有导出语句
  </acceptance_criteria>
  <verify>
    <automated>grep -l "SystemSettings" backend/src/models/SystemSettings.ts backend/src/models/index.ts</automated>
  </verify>
  <done>
    SystemSettings 模型创建完成，可在其他模块导入使用。
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建 settingsService 服务</name>
  <files>backend/src/services/settingsService.ts</files>
  <read_first>
    - backend/src/models/SystemSettings.ts (刚创建的模型)
    - backend/src/services/workService.ts (了解服务模式)
    - backend/src/services/imageService.ts (了解 WatermarkOptions 接口)
  </read_first>
  <action>
    创建 settingsService 提供配置存取功能。

    创建 `backend/src/services/settingsService.ts`:
    ```typescript
    import { AppDataSource } from '../config/database.js';
    import { SystemSettings } from '../models/SystemSettings.js';
    import { Repository } from 'typeorm';

    export interface WatermarkConfig {
      enabled: boolean;
      type: 'text' | 'image';
      text?: string;
      imagePath?: string;
      position: 'top-left' | 'top-right' | 'bottom-left' | 'bottom-right' | 'center';
      opacity: number;
    }

    export class SettingsService {
      private repo: Repository<SystemSettings>;

      constructor() {
        this.repo = AppDataSource.getRepository(SystemSettings);
      }

      // 获取水印配置，返回默认值如果未配置
      async getWatermarkConfig(): Promise<WatermarkConfig> {
        const settings = await this.repo.findOne({
          where: { key: 'watermark' }
        });
        if (!settings) {
          return this.getDefaultWatermarkConfig();
        }
        return JSON.parse(settings.value) as WatermarkConfig;
      }

      // 保存水印配置
      async setWatermarkConfig(config: WatermarkConfig): Promise<void> {
        let settings = await this.repo.findOne({
          where: { key: 'watermark' }
        });
        if (!settings) {
          settings = this.repo.create({
            key: 'watermark',
            value: JSON.stringify(config)
          });
        } else {
          settings.value = JSON.stringify(config);
        }
        await this.repo.save(settings);
      }

      private getDefaultWatermarkConfig(): WatermarkConfig {
        return {
          enabled: false,
          type: 'text',
          text: '',
          position: 'bottom-right',
          opacity: 0.5
        };
      }
    }

    export const settingsService = new SettingsService();
    ```
  </action>
  <acceptance_criteria>
    - 文件 `backend/src/services/settingsService.ts` 存在
    - 导出 WatermarkConfig 接口
    - 导出 settingsService 实例
    - 包含 getWatermarkConfig 和 setWatermarkConfig 方法
  </acceptance_criteria>
  <verify>
    <automated>grep -E "(getWatermarkConfig|setWatermarkConfig|WatermarkConfig)" backend/src/services/settingsService.ts</automated>
  </verify>
  <done>
    settingsService 创建完成，可存取水印配置。
  </done>
</task>

<task type="auto">
  <name>Task 3: 集成水印到上传流程</name>
  <files>backend/src/services/uploadService.ts</files>
  <read_first>
    - backend/src/services/uploadService.ts (当前上传逻辑)
    - backend/src/services/imageService.ts (水印方法)
    - backend/src/services/settingsService.ts (配置服务)
  </read_first>
  <action>
    修改 `backend/src/services/uploadService.ts` 在生成缩略图时应用水印。

    1. 在文件顶部添加导入：
       ```typescript
       import { settingsService, WatermarkConfig } from './settingsService.js';
       ```

    2. 修改 `processImage` 方法，在生成缩略图后应用水印：
       ```typescript
       async processImage(file: Express.Multer.File): Promise<UploadResult> {
         const { dir, monthDir } = getUploadPath();

         // 先生成缩略图
         const thumbnails = await imageService.generateThumbnails(
           file.path,
           dir,
           file.originalname
         );

         // 获取水印配置
         const watermarkConfig = await settingsService.getWatermarkConfig();

         // 如果启用水印，给缩略图添加水印
         if (watermarkConfig.enabled) {
           // 给小缩略图添加水印
           if (thumbnails.small) {
             const wmSmallPath = thumbnails.small.replace('_small', '_small_wm');
             await imageService.addWatermark(thumbnails.small, wmSmallPath, {
               text: watermarkConfig.type === 'text' ? watermarkConfig.text : undefined,
               imagePath: watermarkConfig.type === 'image' ? watermarkConfig.imagePath : undefined,
               position: watermarkConfig.position,
               opacity: watermarkConfig.opacity
             });
             // 用带水印版本替换原缩略图
             fs.unlinkSync(thumbnails.small);
             fs.renameSync(wmSmallPath, thumbnails.small);
           }

           // 给大缩略图添加水印
           if (thumbnails.large) {
             const wmLargePath = thumbnails.large.replace('_large', '_large_wm');
             await imageService.addWatermark(thumbnails.large, wmLargePath, {
               text: watermarkConfig.type === 'text' ? watermarkConfig.text : undefined,
               imagePath: watermarkConfig.type === 'image' ? watermarkConfig.imagePath : undefined,
               position: watermarkConfig.position,
               opacity: watermarkConfig.opacity
             });
             // 用带水印版本替换原缩略图
             fs.unlinkSync(thumbnails.large);
             fs.renameSync(wmLargePath, thumbnails.large);
           }
         }

         return {
           filePath: toUrlPath('uploads/works', monthDir, file.filename),
           thumbnailSmall: toUrlPath('uploads/works', monthDir, path.basename(thumbnails.small)),
           thumbnailLarge: toUrlPath('uploads/works', monthDir, path.basename(thumbnails.large)),
           originalFilename: file.originalname,
           fileType: 'image',
           mimeType: file.mimetype,
           fileSize: file.size,
         };
       }
       ```

    3. 确保导入 fs 模块（用于文件操作）。
  </action>
  <acceptance_criteria>
    - uploadService.ts 导入 settingsService
    - processImage 方法调用 settingsService.getWatermarkConfig()
    - 当 enabled=true 时调用 imageService.addWatermark()
    - 原图文件未被修改（只有缩略图添加水印）
  </acceptance_criteria>
  <verify>
    <automated>grep -E "(settingsService|getWatermarkConfig|addWatermark)" backend/src/services/uploadService.ts</automated>
  </verify>
  <done>
    上传流程集成水印完成，新上传的图片缩略图会根据配置添加水印。
  </done>
</task>

<task type="auto">
  <name>Task 4: 创建配置 API 端点</name>
  <files>backend/src/routes/settings.ts, backend/src/app.ts</files>
  <read_first>
    - backend/src/routes/works.ts (了解路由模式)
    - backend/src/services/settingsService.ts (配置服务)
    - backend/src/app.ts (了解路由注册方式)
  </read_first>
  <action>
    创建水印配置 API 端点。

    1. 创建 `backend/src/routes/settings.ts`:
       ```typescript
       import { Router, Request, Response } from 'express';
       import { settingsService, WatermarkConfig } from '../services/settingsService.js';
       import { successResponse, errorResponse, ErrorCodes } from '../types/response.js';
       import { authMiddleware } from '../middlewares/auth.js';
       import multer from 'multer';
       import path from 'path';
       import fs from 'fs';
       import { v4 as uuidv4 } from 'uuid';

       const router = Router();

       // 水印图片上传配置
       const watermarkStorage = multer.diskStorage({
         destination: (req, file, cb) => {
           const dir = path.join(process.cwd(), 'uploads/watermarks');
           if (!fs.existsSync(dir)) {
             fs.mkdirSync(dir, { recursive: true });
           }
           cb(null, dir);
         },
         filename: (req, file, cb) => {
           const ext = path.extname(file.originalname);
           cb(null, `watermark_${uuidv4()}${ext}`);
         }
       });

       const watermarkUpload = multer({ storage: watermarkStorage });

       /**
        * GET /api/settings/watermark
        * 获取水印配置
        */
       router.get('/watermark', async (req: Request, res: Response) => {
         try {
           const config = await settingsService.getWatermarkConfig();
           res.json(successResponse(config));
         } catch (error: any) {
           res.status(500).json(errorResponse(ErrorCodes.UNKNOWN, error.message));
         }
       });

       /**
        * PUT /api/settings/watermark
        * 更新水印配置
        */
       router.put('/watermark', authMiddleware, async (req: Request, res: Response) => {
         try {
           const config: WatermarkConfig = req.body;

           // 验证必填字段
           if (config.enabled && config.type === 'text' && !config.text) {
             res.status(400).json(errorResponse(ErrorCodes.VALIDATION_ERROR, '文字水印需要提供文字内容'));
             return;
           }
           if (config.enabled && config.type === 'image' && !config.imagePath) {
             res.status(400).json(errorResponse(ErrorCodes.VALIDATION_ERROR, '图片水印需要提供图片路径'));
             return;
           }

           await settingsService.setWatermarkConfig(config);
           res.json(successResponse(config, '水印配置已保存'));
         } catch (error: any) {
           res.status(500).json(errorResponse(ErrorCodes.UNKNOWN, error.message));
         }
       });

       /**
        * POST /api/settings/watermark/image
        * 上传水印图片
        */
       router.post('/watermark/image', authMiddleware, watermarkUpload.single('image'), async (req: Request, res: Response) => {
         try {
           if (!req.file) {
             res.status(400).json(errorResponse(ErrorCodes.VALIDATION_ERROR, '请上传图片文件'));
             return;
           }

           const imagePath = `/uploads/watermarks/${req.file.filename}`;
           res.json(successResponse({ imagePath }, '水印图片上传成功'));
         } catch (error: any) {
           res.status(500).json(errorResponse(ErrorCodes.UNKNOWN, error.message));
         }
       });

       export default router;
       ```

    2. 在 `backend/src/app.ts` 注册路由：
       ```typescript
       import settingsRoutes from './routes/settings.js';
       // ...
       app.use('/api/settings', settingsRoutes);
       ```
  </action>
  <acceptance_criteria>
    - 文件 `backend/src/routes/settings.ts` 存在
    - 包含 GET /watermark 和 PUT /watermark 端点
    - PUT 端点需要认证（authMiddleware）
    - 在 app.ts 中注册路由
  </acceptance_criteria>
  <verify>
    <automated>grep -E "(settingsRoutes|/api/settings)" backend/src/app.ts && grep -E "(get.*watermark|put.*watermark)" backend/src/routes/settings.ts</automated>
  </verify>
  <done>
    水印配置 API 端点创建完成，管理员可通过 API 配置水印。
  </done>
</task>

<task type="auto">
  <name>Task 5: 创建前端配置界面</name>
  <files>frontend/src/api/settings.ts, frontend/src/views/admin/Settings.vue, frontend/src/router/index.ts</files>
  <read_first>
    - frontend/src/api/work.ts (了解 API 模式)
    - frontend/src/views/admin/Works.vue (了解管理页面模式)
    - frontend/src/router/index.ts (了解路由注册)
  </read_first>
  <action>
    创建水印配置管理界面。

    1. 创建 `frontend/src/api/settings.ts`:
       ```typescript
       import request from '@/utils/request';
       import type { WatermarkConfig } from '@/types/settings';

       export const settingsApi = {
         getWatermarkConfig: () =>
           request.get<WatermarkConfig>('/settings/watermark'),

         setWatermarkConfig: (config: WatermarkConfig) =>
           request.put<WatermarkConfig>('/settings/watermark', config),

         uploadWatermarkImage: (file: File) => {
           const formData = new FormData();
           formData.append('image', file);
           return request.post<{ imagePath: string }>('/settings/watermark/image', formData, {
             headers: { 'Content-Type': 'multipart/form-data' }
           });
         }
       };
       ```

    2. 创建 `frontend/src/types/settings.ts`:
       ```typescript
       export interface WatermarkConfig {
         enabled: boolean;
         type: 'text' | 'image';
         text?: string;
         imagePath?: string;
         position: 'top-left' | 'top-right' | 'bottom-left' | 'bottom-right' | 'center';
         opacity: number;
       }
       ```

    3. 创建 `frontend/src/views/admin/Settings.vue`:
       - 使用 Element Plus 组件
       - 表单字段：启用开关、类型选择、文字内容/图片上传、位置选择、透明度滑块
       - 保存按钮调用 settingsApi.setWatermarkConfig()
       - 加载时调用 settingsApi.getWatermarkConfig()

    4. 在 `frontend/src/router/index.ts` 添加路由：
       ```typescript
       {
         path: 'settings',
         name: 'Settings',
         component: () => import('@/views/admin/Settings.vue'),
         meta: { requiresAuth: true, title: '系统设置' }
       }
       ```
  </action>
  <acceptance_criteria>
    - 文件 frontend/src/api/settings.ts 存在
    - 文件 frontend/src/types/settings.ts 存在
    - 文件 frontend/src/views/admin/Settings.vue 存在
    - 路由 /admin/settings 可访问
    - 界面包含启用水印开关、类型选择、位置选择、透明度滑块
    - 保存按钮可提交配置
  </acceptance_criteria>
  <verify>
    <automated>ls frontend/src/views/admin/Settings.vue frontend/src/api/settings.ts frontend/src/types/settings.ts 2>/dev/null && grep -l "Settings" frontend/src/router/index.ts</automated>
  </verify>
  <done>
    前端配置界面创建完成，管理员可在后台配置水印。
  </done>
</task>

</tasks>

<verification>
1. 启动后端服务，验证 API 端点：
   - GET /api/settings/watermark 返回默认配置
   - PUT /api/settings/watermark 保存配置成功
   - POST /api/settings/watermark/image 上传图片成功

2. 前端验证：
   - 访问 /admin/settings 显示配置页面
   - 修改配置并保存，刷新后配置保留

3. 上传测试：
   - 配置启用水印
   - 上传新图片
   - 验证缩略图带有水印，原图无水印
</verification>

<success_criteria>
- [ ] SystemSettings 模型创建并导出
- [ ] settingsService 提供水印配置存取
- [ ] uploadService 在生成缩略图时应用水印
- [ ] API 端点 GET/PUT /api/settings/watermark 工作
- [ ] 前端管理界面可配置水印
- [ ] 新上传的图片缩略图显示水印
</success_criteria>

<output>
After completion, create `.planning/phases/07-bug/07-01-SUMMARY.md`
</output>
---
phase: 07-bug
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/services/publicService.ts
  - backend/src/routes/public.ts
  - frontend/src/views/Home.vue
autonomous: true
requirements:
  - BUG-03

must_haves:
  truths:
    - "用户在公开画廊访问作品详情页时，作品浏览量递增"
    - "浏览量数字准确反映访问次数"
    - "私密分享访问不影响作品浏览量（单独记录）"
  artifacts:
    - path: "backend/src/services/publicService.ts"
      provides: "公开作品服务"
      contains: "viewCount"
    - path: "backend/src/routes/public.ts"
      provides: "公开 API 端点"
  key_links:
    - from: "frontend/src/views/Home.vue"
      to: "/api/public/works/:id"
      via: "作品详情请求"
      pattern: "getPublicWorkById"
    - from: "backend/src/services/publicService.ts"
      to: "workRepo.increment"
      via: "浏览量递增"
      pattern: "increment.*viewCount"
---

<objective>
修复浏览量统计：确保作品浏览量在访问时正确递增。

**Purpose:** 准确统计作品曝光度，帮助摄影师了解作品热度。

**Output:**
- 公开访问作品详情时浏览量递增
- 浏览量统计准确
</objective>

<execution_context>
@C:/workspace/photo-show/.opencode/get-shit-done/workflows/execute-plan.md
@C:/workspace/photo-show/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-bug/07-CONTEXT.md
@.planning/research/SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From backend/src/services/publicService.ts:
```typescript
async getPublicWorkById(id: string): Promise<Work | null> {
  const work = await this.workRepo.findOne({
    where: { id, isPublic: true },
    relations: ['albums', 'tags', 'mediaItems'],
    // ...
  });

  if (work) {
    // Increment view count
    await this.workRepo.increment({ id }, 'viewCount', 1);
  }

  return work;
}
```

From backend/src/routes/public.ts:
```typescript
router.get('/works/:id', async (req: Request, res: Response) => {
  const id = req.params.id as string;
  const work = await publicService.getPublicWorkById(id);
  // ...
});
```

From frontend/src/views/Home.vue (expected pattern):
- 作品列表显示
- 点击作品进入详情（使用路由或弹窗）
- 需要确认是否调用了 /api/public/works/:id
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 验证浏览量递增逻辑</name>
  <files>backend/src/services/publicService.ts</files>
  <read_first>
    - backend/src/services/publicService.ts (当前实现)
    - backend/src/routes/public.ts (API 端点)
    - frontend/src/views/Home.vue (前端画廊)
  </read_first>
  <action>
    验证并修复浏览量递增逻辑。

    **诊断步骤：**
    1. 确认 `publicService.getPublicWorkById()` 的 viewCount 递增代码存在
    2. 确认前端是否调用了 `/api/public/works/:id` 端点
    3. 检查是否有缓存导致递增不执行

    **检查点：**
    - `getPublicWorkById` 方法中有 `await this.workRepo.increment({ id }, 'viewCount', 1)`
    - 前端作品详情页调用了此 API

    **如果问题存在：**

    问题1: 前端没有调用作品详情 API
    修复: 确保前端在显示作品详情时调用 `/api/public/works/:id`

    问题2: 作品列表已经包含所有信息，前端不再请求详情
    修复: 添加专门的"记录浏览"端点，或在列表加载时统计

    问题3: increment 方法执行但数据库未更新
    修复: 检查数据库连接和事务

    **可选优化：**
    如果前端使用弹窗显示详情而不调用详情 API，添加浏览记录端点：
    ```typescript
    // 在 backend/src/routes/public.ts 添加
    router.post('/works/:id/view', async (req: Request, res: Response) => {
      const id = req.params.id as string;
      await publicService.incrementViewCount(id);
      res.json(successResponse(null, 'View recorded'));
    });
    ```
  </action>
  <acceptance_criteria>
    - getPublicWorkById 方法包含 viewCount 递增代码
    - 前端调用作品详情 API 或浏览记录端点
    - 数据库 viewCount 字段正确递增
  </acceptance_criteria>
  <verify>
    <automated>grep -A2 "increment.*viewCount" backend/src/services/publicService.ts</automated>
  </verify>
  <done>
    浏览量递增逻辑验证完成，确保每次访问正确递增。
  </done>
</task>

<task type="auto">
  <name>Task 2: 确保前端正确触发浏览统计</name>
  <files>frontend/src/views/Home.vue, frontend/src/api/public.ts</files>
  <read_first>
    - frontend/src/views/Home.vue (画廊页面)
    - frontend/src/components/gallery/WorkCard.vue (作品卡片)
    - frontend/src/components/gallery/Lightbox.vue (灯箱组件)
    - frontend/src/api/public.ts (公开 API)
  </read_first>
  <action>
    确保前端在查看作品详情时触发浏览统计。

    1. 检查 `frontend/src/views/Home.vue` 的作品详情显示方式：
       - 是否使用 Lightbox 弹窗？
       - 是否导航到详情页？
       - 是否调用 `/api/public/works/:id`？

    2. 如果使用 Lightbox 弹窗显示详情：
       - 在打开 Lightbox 时调用浏览统计 API
       - 或者确保 Lightbox 加载作品详情时触发后端统计

    3. 检查 `frontend/src/api/public.ts`：
       ```typescript
       export const publicApi = {
         getWork: (id: string) =>
           request.get(`/public/works/${id}`),

         // 如果需要单独的浏览记录
         recordView: (id: string) =>
           request.post(`/public/works/${id}/view`)
       }
       ```

    4. 修改 `frontend/src/views/Home.vue` 或相关组件：
       ```typescript
       // 在打开作品详情时
       async openWorkDetail(workId: string) {
         // 调用详情 API，后端自动递增 viewCount
         const work = await publicApi.getWork(workId);
         // 显示详情...
       }
       ```
  </action>
  <acceptance_criteria>
    - 查看作品详情时调用 /api/public/works/:id
    - 或者调用专门的浏览记录端点
    - 浏览量在数据库中正确递增
  </acceptance_criteria>
  <verify>
    <automated>grep -E "getWork|public/works" frontend/src/views/Home.vue frontend/src/components/gallery/*.vue 2>/dev/null | head -20</automated>
  </verify>
  <done>
    前端正确触发浏览统计，每次查看作品详情都会递增浏览量。
  </done>
</task>

<task type="auto">
  <name>Task 3: 验证私密分享访问不影响作品浏览量</name>
  <files>backend/src/services/shareService.ts, backend/src/routes/share.ts</files>
  <read_first>
    - backend/src/routes/share.ts (私密分享路由)
    - backend/src/services/shareService.ts (私密分享服务)
    - backend/src/services/accessLogService.ts (访问日志)
  </read_first>
  <action>
    验证私密分享访问与公开浏览量统计分离。

    1. 检查 `backend/src/routes/share.ts` 的 GET /:token 端点
       - 确认不调用 publicService.getPublicWorkById()
       - 确认使用 accessLogService 记录访问

    2. 验证访问日志记录逻辑：
       - 私密分享访问记录到 ShareAccessLog
       - 不影响 Work.viewCount

    3. 如果发现问题，确保分离逻辑：
       ```typescript
       // 私密分享访问 - 只记录日志，不递增 viewCount
       router.get('/:token', async (req, res) => {
         const shareData = await shareService.validateToken(token);
         const works = await workService.getWorkByIds(shareData.workIds); // 不递增 viewCount

         // 记录私密访问日志
         await accessLogService.recordAccess(token, workId, 'view', req);

         res.json(successResponse({ works }));
       });
       ```

    这是已实现的行为，只需要验证正确性。
  </action>
  <acceptance_criteria>
    - 私密分享访问不递增 Work.viewCount
    - 私密分享访问记录到 ShareAccessLog
    - 公开访问和私密访问统计分离
  </acceptance_criteria>
  <verify>
    <automated>grep -E "accessLogService|recordAccess" backend/src/routes/share.ts</automated>
  </verify>
  <done>
    私密分享访问统计验证完成，与公开浏览量正确分离。
  </done>
</task>

</tasks>

<verification>
1. 测试浏览量递增：
   - 访问公开画廊
   - 点击查看作品详情
   - 检查数据库 works 表 viewCount 字段是否递增
   - 刷新页面再次查看，viewCount 应继续递增

2. 测试私密分享不影响浏览量：
   - 创建私密分享链接
   - 在隐私模式访问分享链接
   - 检查作品的 viewCount 不应变化
   - 检查 share_access_logs 表有访问记录

3. 前端验证：
   - 打开浏览器开发者工具 Network 面板
   - 查看作品详情时确认有 /api/public/works/:id 请求
</verification>

<success_criteria>
- [ ] 公开访问作品详情时 viewCount 正确递增
- [ ] 私密分享访问不影响 viewCount
- [ ] 浏览量统计准确反映公开访问次数
</success_criteria>

<output>
After completion, create `.planning/phases/07-bug/07-03-SUMMARY.md`
</output>